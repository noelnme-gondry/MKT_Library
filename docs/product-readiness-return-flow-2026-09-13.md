# 제품 준비·재방문 개선 — 2026-09-13

시작 기준: `fc48a40e` (`main`, #867). 경쟁 제품의 현재 기능·가격이나 실제 사용자 유지율을 비교한 보고서가 아니다. 앞선 감사의 결론을 그대로 기능 목록으로 바꾸지 않고, 현재 코드에서 재현된 문제와 실행 가능한 조치를 반영했다.

## 반영 내용

| 확인한 문제 | 변경 | 확인 방법 |
|---|---|---|
| 사용자 지정 집계의 시작일만 검사해 현재 기간과 겹치는 과거 집계, 과거끼리 겹치는 집계가 평소 변동 범위에 중복 포함됨 | 같은 통화·길이의 과거 기간을 최신순으로 고르되 중복 기간을 제외. 같은 시계열에서 판정 배열과 화면 근거를 파생 | 수정 전 `[10,20,30,40]`, 수정 후 `[10,30]` 회귀 테스트. 기존 주간 계산 테스트 유지 |
| 모든 과거 값이 같아 변동 폭을 추정할 수 없어도 “비교 이력이 짧다”고 안내 | 부족한 이력과 동일한 값의 이력을 구분. 실제 사용한 기간·KPI 값은 접힌 근거 표에 제공 | KO/EN 스모크. 판정과 같은 최근 8개 유효값만 표시하며 누락값은 제외 |
| 사용자 지정 기간도 주 단위로 표시하고, 저장 건수를 실제 판정 표본 수처럼 안내 | “비교 가능한 기간”과 “이번 판정에 쓴 이력”으로 구분 | 최대 16개 저장 레코드와 최대 8개 판정 기간을 혼동하지 않도록 확인 |
| Sheets 접근 실패 시 비공개 자료의 공개 전환을 권유 | 공개 자료 전용 입력임을 명시하고, 권한을 유지한 CSV 업로드 경로 안내. 최근 연결 목록과 가져온 데이터 저장 설정도 구분 | KO/EN 입력 도움말의 ARIA 연결, 권한 오류, 기존 API 키/CSV export 입력 테스트 |
| 예측 예산을 표시 통화로 입력받아 고정 환율로 역환산하고, 소수점이 입력 중 삭제됨 | 총예산·채널 최소/최대·채널별 예산을 원본 통화로 입력. 소수점·천 단위 구분 금액을 보존 | 제약 입력 콜백의 실제 금액과 빈값 의미 확인. 실제 예측 화면에서 USD 원본 예산이 KRW 표시 전환 후에도 동일함을 확인 |
| 운영 설정의 준비 여부와 실제 결제·메일 성공을 혼동할 위험 | 공개 GET만 사용하는 재확인 스크립트 추가. 응답 전체나 이메일을 출력하지 않고 허용된 상태만 기록 | 비활성·활성·요청 실패 테스트. 실패는 false로 위장하지 않고 미확인 처리 |

새 통계 엔진·AI 해석·서버 광고 데이터 저장을 추가하지 않았다. 기존 수학 엔진과 골든값은 유지한다. 결과 금액의 환산은 여전히 고정 참고 환율이며, 예산 입력과 구분한다.

## 재방문·유입 측정

기존 계측은 이미 첫 입력, 매핑, 실제 결과 노출, 명시적 결정 저장, 다음 데이터 비교를 구분한다. 새 이벤트를 중복으로 추가하지 않았다. 정의와 GA 탐색 절차는 [ga4-product-events.md](ga4-product-events.md)의 정본을 따른다.

- 유입→사용: 실제 데이터(`source != demo`)의 입력 성공 → 매핑 확인 → 결과 노출을 본다. 샘플 체험과 합산하지 않는다.
- 사용→재검토: 명시적 결정 저장 → 이후 세션의 저장 집계 비교 → 실제 결과 노출 → 결정 검토 완료를 본다. 자동 집계 저장은 결정 저장으로 세지 않는다.
- `visit_type=with_history`는 기록 보유 상태이며 재방문·D7 리텐션 자체가 아니다. GA 사용자·세션·날짜와 함께 집계해야 한다. 이 작업에서 실제 고객 행동률은 측정하지 않았고 개선 효과를 숫자로 주장하지 않는다.
- 기존 브라우저 테스트에서 글→실제 CSV→결정→복사→새로고침→다음 기간 CSV 흐름을 실행하고, 새 근거 표의 날짜·값·접근성·가로 넘침과 원본 데이터의 외부 요청 유출 여부를 추가 확인한다.

## 공개 준비 상태

2026-09-13 20:39 KST 공개 응답: 결제 `enabled=false`, `mode=test`; 계정 `enabled=true`, `signupRestricted=true`; 메일 `mailEnabled=true`. 마지막 값은 설정 상태이며 실제 수신을 뜻하지 않는다. 운영 설정은 변경하지 않았다.

재확인:

```sh
cd v2-migration
node scripts/check-public-readiness.mjs
node scripts/check-public-readiness.mjs --expect-public
```

기본 명령은 응답을 정상적으로 확인하면 종료 코드 0이다. 공개 전환 확인 명령은 라이브 결제·공개 계정 접근·메일 설정이 모두 확인되지 않으면 종료 코드 1이다. 두 명령 모두 실거래·복원·메일 발송을 수행하지 않는다.

공개 전환은 운영 의도가 정해진 뒤 일반 계정 가입·로그인, 승인된 실거래, 새 세션의 이용권 복원, 영수증 수신, 알림 동의·해제와 예정일 처리까지 확인해야 끝난다. 계정 생성·결제·실제 메일 수신은 이번 검증에 포함하지 않았다.

## 나머지 감사 항목의 처리

| 항목 | 현재 처리 및 필요한 근거 |
|---|---|
| 스냅샷 활용·맞춤 지표/차트·분석 추천·백업·보고서 공유·모바일 | 기존 구현을 유지하고 관련 회귀 검사. “없다”거나 새로 만들었다고 주장하지 않음 |
| 광고/비공개 Sheets OAuth·자동 수집 | 반복해서 쓰는 실제 소스, 권한·토큰 수명·브라우저 직접 접근 범위를 정해야 함. 프라이버시 때문에 모든 편의가 불가능하다는 주장은 채택하지 않음 |
| 지표 알림·정기 리포트·Slack/Teams | 최신 데이터를 얻는 경로와 발송 대상·동의가 먼저 필요. 날짜 알림 큐가 있다는 이유로 지표 감시까지 완료로 취급하지 않음 |
| 기기 간 동기화·팀 권한·협업 | 백업/복원은 이미 가능. 자동 동기화와 역할별 권한은 별도 설계가 필요하며 파일 공유로 대체했다고 주장하지 않음 |
| 고객 사례·실명/팀 소개·벤치마크 | 동의받은 실제 사례·소개 내용과 사용권이 있는 비교 자료가 필요. 임의 수치·로고·경력·업계 평균을 추가하지 않음 |
| 인프로덕트 LLM | 데이터 전송 범위·근거 추적·판단 보류 계약이 선행. 기존 규칙 기반 설명이 있다는 사실과 구분 |
| AEO·EN 검색 유입 | SSOT 생성기를 실행해 현재 목록 KO 24개·EN 24개(총 48개)를 확인. 실제 AEO 응답 측정과 최신 GSC 자료는 미확인. 8월 자료를 9월 성과로 쓰거나 빈 실행 결과를 채우지 않음 |
| 가격·무료 프로젝트 수·체험 범위 | 변경 효과가 입증되지 않아 기존 상품 계약 유지. 실제 체험·지불 의향 측정 후 판단 |

## 검증 기록

- 로컬 전체 golden+smoke: 3,687개 통과, 2개 건너뜀. 이후 손상된 기간 타입 방어 테스트 추가 후 관련 검사 재실행. 로컬 수에는 보존한 사용자 미추적 테스트가 포함되므로 PR CI 수와 다를 수 있다.
- lint·프로덕션 빌드 통과.
- 새 이력 근거를 포함한 첫 업로드→저장→재방문 브라우저 검사 4개 통과(320/768/1440px KO, 데스크톱 라이트 EN).
- 전체 브라우저 검사와 최종 커밋 CI 결과는 PR에서 확인한다. 육안 스크린샷 검사는 수행하지 않았다.
- 기존 사용자 변경·미추적 자료는 수정하거나 커밋하지 않는다.
