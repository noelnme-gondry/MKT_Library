# Claude 변경·결제·GA 감사 — 2026-09-19

> 아래 상태는 9월 19일 감사 시점 기준이다. 이후 사용자 승인으로 수정·검증·PR 반영을 진행했으며, 최신 결과는 [후속 수정 보고서](project-review-workflow-fixes-2026-09-20.md)를 따른다.

## 범위와 상태

- 이전 Codex 작업의 squash 기준 `98bc2f6d` 이후 `8e3d1bad`까지 PR #878–#894, 213개 변경 파일을 대상으로 변경 목록·주요 동작·회귀를 검토했다.
- 계정/체험, 프로젝트 저장/검토, 매핑 복원, GA, 결제 경로를 집중 검증했다. 수학/파싱 변경과 호출부도 점검했지만 이번 작업에서 수학 엔진·골든값은 수정하지 않았다.
- 전체 변경의 모든 CSS·문서·테스트 줄을 수작업으로 검토한 전수 감사는 아니다. 모바일/실브라우저 전체 화면의 시각 검증도 하지 않았다. 아래는 확인하고 수정한 문제와 실제 관측 결과다.
- 원본 작업 폴더는 보존하고 `/tmp/growthopt-audit-0919`의 `codex/nicepay-analytics-audit-0919`에서 수정했다. 커밋·push·PR·배포·운영 DB 변경은 하지 않았다.

## 확인하고 수정한 문제

| 우선순위 | 문제와 재현 조건 | 수정·검증 |
|---|---|---|
| P1 | 한글 검토 이력을 여러 회 저장하면 URL 인코딩된 `episodes`가 6,000자에서 잘려 과거 검토·최신 결과가 소실됨 | 회차 수와 각 필드의 실제 상한에서 저장 한도를 파생. 긴 한글 이력 왕복 테스트 |
| P1 | 기존 저장 데이터의 스키마 표식이 없으면 매핑을 전부 재추천해 사용자의 수동 매핑·무시 선택을 덮어씀 | 유효한 매핑과 무시 선택 보존, 더 이상 존재하지 않는 키만 재인식. 영향받은 컬럼의 바인딩만 해제 |
| P1 | 체험 시작 조건이 첫 프로젝트로 바뀌었는데 리뷰 저장 창은 계정 메모 저장을 안내하여 서버의 `PRO_REQUIRED`에 막힘 | 프로젝트 체험 관문으로 연결하고 작성 중 초안 유지. 실제 체험 요청 경로 KR/EN 스모크 |
| P1 | 주간 리뷰의 기존 편집기가 읽기 전용 결정 목록으로 교체되며 실제 결과·배운 점 입력 및 내보내기 진입점이 사라짐 | 결정 목록에서 기존 편집기 열기 복원. 실제 결과 입력 DOM까지 KR/EN 검증 |
| P2 | 실제 저장 성공 후 목록 갱신/후속 콜백이 실패하면 저장 실패로 안내하고 `project_review_save_failed`를 전송 | 저장 완료 시점에 성공 이벤트 확정. 후속 갱신 실패는 저장됨+경고로 분리 |
| P2 | 새 시트로 연결한 뒤 업로더가 닫히면 새로고침 참조가 사라져 버튼이 아무 일도 하지 않음 | 시트 연결 중 업로더를 숨겨 유지. 닫힌 상태에서 새로고침 실제 호출 KR/EN 검증 |
| P2 | 샘플에서 실제 계정의 결정 메모를 불러와 섞어 보여줌 | 샘플은 계정 조회 및 표시 제외. 실제 화면→샘플 전환 시 이미 불러온 메모도 제외 |
| P2 | 수동 컬럼 규칙을 파일로 내보내고 가져오면 규칙 출처가 사라져 수동 규칙으로 다시 적용되지 않음 | 알려진 수동 규칙 출처를 직렬화·복원하고 왕복 검증 |
| P2 | 마이페이지의 프로젝트 저장소 읽기 실패를 프로젝트 0개로 표시 | 0 대신 미확인 상태와 오류 안내. KR/EN 실패 경로 검증 |
| P2 | 제품 내부 `source`가 GA 획득 소스와 충돌할 수 있으며 실제 보고서에 `blog`, `route`, `subscription_page`가 유입 소스로 나타남 | 외부 전송 직전에 `interaction_source`로 변경. 내부 호출 계약 유지, 기존 이벤트 전송 스모크 갱신 |
| P2 | 이벤트에 넘긴 `count`, `tool_ids`가 허용 목록에서 누락되고 신규 분석 도구의 종류 구분이 없음 | 안전한 정수·알려진 도구 ID만 허용. ASO/생존/구성 변화 분석 종류 추가 |
| P2 | 결제 확인 중 취소가 확인되어도 예외와 트랜잭션 롤백으로 회수 상태가 저장되지 않을 수 있음 | 권위 있는 PG 취소 상태는 저장 후 구매 미완료 응답. NICEPAY 취소·재조회 회귀 포함 |

마이페이지 이메일/이용권 표현도 현재 로그인·일회 결제 방식에 맞췄다. 기존 이용자의 매핑이나 기록 자체를 일괄 수정하지 않았다.

### 후속 요청에서 추가 수정한 문제

| 우선순위 | 재현 조건·문제 | 수정·검증 |
|---|---|---|
| P2 | 매핑 규칙 저장 실패에도 입력 초안·CSV 헤더 후보를 지워 재시도가 어려움 | 저장 성공 때만 입력을 정리. 실패 뒤 초안 보존 스모크 |
| P2 | 매핑 저장소 조회 실패를 ‘저장한 매핑 없음’으로 안내 | 로딩·실패·빈 결과 구분. 기존 규칙 존재 여부 미확인 안내 KR/EN |
| P2 | 로그인 완료 메시지가 중복 도착하면 체험 요청과 프로젝트 생성 후속 콜백을 중복 호출 | 진행 중·완료 상태로 중복 실행 차단. 수정 전 2회, 수정 후 1회 호출 검증 |
| P2 | 프로젝트의 브랜딩이 비동기로 도착하면 폼이 빈 초기값에 머무름 | 실제 프로젝트 로딩 완료 때 폼 초기화. 저장된 값 표시 KR/EN 검증 |
| P2 | 설문 응답은 저장됐으나 응답이 유실되면 재시도 시 새 ID로 중복 적재 | 같은 답변 재시도에는 동일 제출 ID 사용. 응답 유실 재현 |
| P2 | 분석 목록의 열기 버튼이 존재하지 않는 패널 ID를 `aria-controls`로 가리킴 | 열린 실제 패널만 연결. KR/EN 전체 버튼 순회 검증 |
| P2 | 숫자가 아닌 가드레일 기준값을 입력하면 경고 없이 조건을 버리고 저장 | 잘못된 숫자는 저장 전 안내, 초안 보존. KR/EN 수정 전 실패·수정 후 통과 |

NICEPAY 구현도 재점검하여 테스트 모드 API를 공식 샌드박스 주소로 분리하고 주문 모드와 설정의 불일치를 차단했다. 이 항목은 Claude 변경의 결함이 아니라 이번 NICEPAY 구현 검증에서 보강한 내용이다.

### 변경 묶음별 검토·검증 근거

| Claude PR | 주요 영역 | 확인 근거 |
|---|---|---|
| #878–879 | 결제 고지·심사 로그인·계정 화면 | 비밀번호 해시/허용 계정/세션 경로 검토, 계정·결제 단위 테스트, 구매 진입 E2E |
| #880–881 | 프로젝트·Pro 관문·목표/가드레일·매핑 | 저장/체험 실제 호출 및 렌더 스모크, 프로젝트/계정/유료 기능 E2E, 추가 실패 경로 회귀 |
| #882–883 | 크기·반경·차트·행 수 표시 | 스타일 변경 목록 및 관련 렌더 경로, 모바일/태블릿/데스크톱 진입 E2E. 모든 화면의 육안 디자인 승인은 하지 않음 |
| #884 | 결정 이력 누적 | 인코딩 한도·왕복 저장 회귀, 주간 리뷰 편집/내보내기 연결 스모크 |
| #885–886 | 분석 후 연결·업로드·추천 도구 | 분석 게이트·CSV 적합성 경로 검토, 업로드/운영 흐름 E2E, 패널 접근성 회귀 |
| #887 | 라이트 테마 별칭 | 토큰 변경 및 영어 라이트 화면 E2E. 모든 조합의 시각·대비 전수 측정은 아님 |
| #888–890 | 파싱·지표·스키마 복원·Simpson 해석 | 공유 파서/호출부/판정 경로 검토, 전체 수학·메타모픽·픽스처 테스트, 사용자 매핑 보존 회귀 |
| #891 | 리뷰 이벤트·계정 현황 | 실제 저장 경계 이벤트, 저장소 읽기 실패, GA 설정/실제 보고서 확인 |
| #892–893 | 유입 설문 | 서버 입력/중복 키·클라이언트 재시도 검토, 설문 스모크·기기 크기별 브라우저 검사 |
| #894 | 하네스 | 변경 범위 확인. 하네스 자체는 수정하지 않음 |

수정의 핵심 파일은 `decisionReview.js`, `useDataStore.js`, `ReviewSaveDialog.jsx`, `DecisionHistoryList.jsx`, `ProjectCreateGate.jsx`, `UserMappingSettings.jsx`, `ProjectsPage.jsx`, `SourceSurveyPopup.jsx`, `ToolIndex.jsx`, `DecisionReview.jsx`, `analytics.js`, `paymentServer.js`, `nicepayServer.js` 및 NICEPAY 반환/웹훅 라우트다.

## GA 관리자에서 반영한 내용

대상: MKT Library / growthoptplaybook, 계정 `398682209`, 속성 `542428327`, 스트림 `G-DK12TNR0GW`.

- `purchase`: 이미 주요 이벤트였음.
- `project_review_saved`: 주요 이벤트로 추가. 이벤트당 집계, 임의 금전 가치 없음.
- 이벤트 범위 맞춤 측정기준 `interaction_source`, `analysis_type`, `state` 신규 등록·저장 확인. 과거 `source` 정의는 유지.
- 최근 28일 활성 이벤트 59종 확인. `analysis_started/completed/result_viewed`, `data_import_start/success`, `login_started/completed`, `trial_started`, `weekly_decision_saved`, `weekly_review_completed` 등은 실제 스트림 활동이 있음.
- `purchase`와 `project_review_saved`는 확인 시점 최근 28일 활성 스트림 없음. 등록 성공이 실제 수신 성공을 뜻하지 않는다.
- 기존 주요 이벤트에는 `project_limit_reached`도 포함되어 있다. 이는 성공 전환이 아니라 한도 도달이므로 전환 성과 집계에서는 별도 해석해야 한다. 기존 설정을 임의 삭제하지 않았다.
- 스트림의 브라우저 기록 기반 페이지 조회는 꺼져 있었고, 공개 GTM `GTM-T6C7QW75`의 현재 태그·규칙은 비어 있었다. 이 두 경로의 페이지뷰 중복 설정은 확인되지 않았다.

구매 이벤트는 서버 확인 후 브라우저에서 보내므로 광고 차단·탭 종료 등으로 누락될 수 있다. GA를 매출 원장으로 사용하지 않는다. 배포 뒤 새 거래번호의 실제 구매 및 리뷰 저장 1회씩을 DebugView/실시간과 대조하는 최종 검증은 남아 있다. 이번 조사에서 가짜 구매 이벤트나 사용자 CSV를 전송하지 않았다.

## 싱가포르 트래픽 관측

아래 날짜는 해당 GA 보고서에 설정한 날짜 기준이다. 사용자가 말한 ‘어제 약 600명’과 동일한 지표·기간은 재현하지 못했다.

| 날짜 | 싱가포르 활성 사용자 | 신규 사용자 | 관측 |
|---|---:|---:|---|
| 9월 16일 | 1 | 1 | 이벤트 3개 |
| 9월 17일 | 381 | 381 | 세션 380, 참여 세션 155, 이벤트 2,535, 평균 참여 약 6초 |
| 9월 18일 | 1 | 1 | 이벤트 7개 |
| 9월 12–18일 | 386 | 386 | 이벤트 2,566 |

9월 17일 싱가포르 상세:

- 방문 페이지 368종/세션 380개. 루트 3세션, 일부 페이지 2세션, 대부분 거의 한 번씩 방문한 형태.
- 브라우저 Chrome 381명(100%), OS 버전 Macintosh Intel 10.15 381명(100%).
- Direct 348세션(91.58%), Unassigned 31세션(8.16%). 보고서 전체 380과 표 행 합계 379는 일치하지 않아 임의로 1세션을 채우지 않았다.
- 세션 소스/매체: `(direct)/(none)` 348, `blog/(not set)` 27, `subscription_page/(not set)` 2, `glossary/(not set)` 1, `route/(not set)` 1.
- 주요 이벤트 0. 다만 당시에 리뷰 저장 주요 이벤트는 아직 등록 전이므로 이를 사용 의도 없음의 단독 증거로 쓰지 않는다.

**판단: 자동 브라우저로 페이지를 순회한 크롤링/모니터링 트래픽이 강하게 의심된다.** 같은 환경으로 수백 페이지를 거의 한 번씩 방문한 패턴에 근거한 추정이며, 특정 봇·업체·IP/ASN은 미확인이다. 일반 사용자가 섞였을 가능성을 배제하지 않는다. 내부 이벤트 출처로 오염된 획득 보고서 때문에 유입 채널을 그대로 신뢰할 수도 없다.

정확한 발신 주체를 식별하려면 해당 시간대 서버/프록시 로그의 IP·User-Agent·요청 간격과 대조해야 한다. 이 작업에서는 운영 요청 로그의 과거 보존/접근 가능 여부를 확인하지 못했다. 싱가포르 전체 차단이나 GA 과거 데이터 삭제는 하지 않았다.

## NICEPAY와 검증 한계

[NICEPAY 전환 절차](nicepay-migration-2026-09-19.md)에 승인 모델, 환경 설정, DB 변경, 콜백/웹훅, 테스트 및 전환 순서를 정리했다. 구현은 Server 승인+Basic 인증의 카드 일회 결제이며 기존 Toss 주문 조회도 유지한다. 실제 계약/발급 키의 모델은 미확인이다.

로컬 PG 응답 모의 테스트로 소유권·금액·서명·중복 승인 방지·시간 초과 망취소·부분 취소 회수·기존 Toss 복원과 KR/EN 화면 분기를 검사했다. 실제 PG 테스트 키/카드, 모바일 카드사 앱 복귀, 실 DB migration, 외부 웹훅 수신, 실결제/환불은 아직 검증하지 않았다.

후속 브라우저 검증: 12개 E2E 사양 파일에서 142개 통과, 기기별 실행 조건에 따른 2개 skip. 프로젝트·계정·저장/복원·유료 관문·설문·리뷰·업로드·운영 흐름의 모바일/태블릿/데스크톱 및 영어 라이트 경로를 포함한다. PG 응답은 모의이며 실제 카드사 앱 복귀 검증은 아니다. GA 필드 변경에 맞춰 기존 E2E 기대값을 수정했고, 설문 위치 측정은 진입 애니메이션 종료 후 동일 허용 오차로 검사한다.

최종 자동 검증: `npm run test:all` **493개 파일·4,152개 테스트 통과**, 기존 skip 1개 파일·2개 테스트. `npm run lint`, `npm run build`, `git diff --check` 통과. 실제 PG 및 운영 GA 최종 수신 검증은 남아 있다.

최종 fetch 시 `origin/main=8e3d1bad`, 작업 브랜치 HEAD와 원격 main의 커밋 차이는 0/0이며 변경은 미커밋 상태다. 원본 폴더의 `codex/checkout-presentation` 브랜치와 기존 미추적 파일을 보존했다.
