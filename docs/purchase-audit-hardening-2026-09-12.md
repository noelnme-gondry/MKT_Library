# 구매 동선 감사 후속 대조

기준: `0d6310df` (#861). 입력 감사의 `9ff49eb` 이후 수정도 포함해 대조했다. 이 작업은 원본 CSV·계정 메모 삭제·실거래·실환불을 실행하지 않는다.

| 항목 | 대조 및 이번 처리 |
| --- | --- |
| P0-1 지연 입금 | 입금 대기 응답·주문번호·재조회 UI 추가. 대기 단계에는 Pro 미지급, 장기 소유권 쿠키로 이후 입금 조회 가능. 계정 연결 주문은 계정으로도 조회. 기존 익명 주문의 모든 신원 소실은 자동 복구 불가. 실제 입금·다른 인앱 브라우저 실측은 미검증. |
| P0-2 쿠키 소실 | 연결 계정으로 승인·복원 가능. 입력된 잘못된 복원 코드를 계정으로 우회하지 않음. 아직 연락처 없는 기존 익명 주문에는 지원 절차 필요. |
| P0-3 메일 | 신규 주문은 검증 계정 필수, account_id를 주문과 원자적으로 저장. production 시작 명령의 내부 worker가 기동 10초 후 및 15분마다 영수증·옵트인 알림을 처리한다. 별도 스케줄러/새 비밀값은 필수가 아니다. 실배달은 별도 검증. |
| P0-4 운영 테스트 결제 | production에서 테스트 키 신규 주문·승인 금지, 기존 테스트 권한 조회도 null. UI만 숨기는 우회 가능한 차단이 아님. 라이브 키는 변경하지 않음. |
| P0-5 콜백 JSON | Google/이메일 실패 모두 KR/EN HTML·익명 분석 링크·opener 실패 알림. 다른 브라우저 링크의 보안 검증은 유지. |
| P1-6·7·10 | #861에서 체험 진입, 체험/구매 구분·종료일·남은 일수, 요금제 상단 정리 완료. 파일럿 비대상에게 무료 체험 가입을 유도하지 않음. |
| P1-8 claim-pass | 구매 없음은 NO_PURCHASE, Pro 필요와 구분. 구매 권한이 없는 사용자에게 연결 버튼 미노출. |
| P1-9 계정 권한 | 동일 만료일의 기존 구매 플래그 보존. 계정 권한 검증 유예 72시간 상한, 실제 만료를 넘지 않음. 계정 저장 API는 매번 서버 권한 검사. |
| P1-11 로그인 정책 | 사용자 확정: 신규 결제 로그인 필수. 허용목록·운영 변수로 익명 신규 구매를 열 수 없다. 기존 익명 실구매 복원은 유지. |
| P1-12 계측 | 위젯 준비에는 주문/checkout 의도 이벤트 없음. 실제 결제 클릭 뒤 주문 생성·begin_checkout 기록. 결제 버튼 수는 늘리지 않음. |
| P2-13 원천 조회 | 최근 5분 검증 재사용. 공급자 조회 실패에만 마지막 성공 검증 기준 72시간 이내 캐시, 만료·확인된 취소에는 적용 안 함. |
| P2-14 웹훅 | 기존 레이트리밋·영속 주문 확인·Toss 원천 재조회 유지. 별도 웹훅 인증 설정은 미구성. 제공 이벤트 종류에 맞는 인증을 확인한 뒤 설정해야 하며 임의 HMAC 서명을 가정하지 않음. |
| P2-15 허용목록 제외 | 명시 ACCOUNT_RESTRICTED와 데이터 미삭제 안내. 계정 권한 캐시는 제한 확인 시 제거. |
| P2-16 수동 환불 | 기존 정책·지원 절차 유지. 자동 환불 실행은 구현·운영 검증하지 않음. |
| P2-17 보류 응답 | 모달을 강제로 닫지 않고 감사 안내 표시, 같은 모달에서 중복 응답 방지. |

## 확정 정책과 배포

- 조기 재구매는 현재 만료일(활성 체험 포함) 뒤 1개월, 만료 후 구매는 승인부터 1개월. 주문별 starts_at/expires_at은 최초 승인에만 배정한다. 계정 단위 트랜잭션 잠금으로 동시 승인을 직렬화하고 HTTP 동안 잠금을 잡지 않는다.
- 만료로 기록을 삭제하지 않는다. Pro는 즉시 잠그고 기존 열람·내보내기·백업은 유지한다. 7일 후 삭제 제안은 취소됐다. 브라우저 저장소 자체의 기존 90일 미사용 정리는 별도다.
- 라이브 키는 사용자가 별도로 설정한다. 실입금·실거래·실환불·실메일 수신·인앱 복귀는 운영 검증이며 테스트 통과로 대체하지 않는다. 수동 환불 정책은 유지한다.
- npm start의 prestart가 scripts/payment-periods.sql을 트랜잭션으로 실행한다. nullable starts_at 추가와 gop_paid_until 함수만 도입하며 기존 승인·만료일·기록은 보존한다. 실패하면 앱 기동을 중단한다. 재실행 가능하다.
- 롤백: 연장 주문 생성 후 #861 이전 바이너리로 되돌리지 않는다(이전 재조회가 만료일을 줄임). 판매만 PAYMENTS_LIVE_ENABLED=false로 차단하고 새 원장 reader/writer를 유지하는 forward fix를 사용한다. 열·함수·기록을 삭제하지 않는다.
- 내부 메일 worker는 개발/빌드에서 실행하지 않는다. ACCOUNT_MAIL_ENABLED=false로 중단 가능하다. 외부 jobs 경로는 선택적이며 기존 인증을 유지한다. DB lease로 다중 인스턴스를 조정한다.
- Railway PR/staging 환경은 상속된 설정이 있어도 계정 기능·DB migration·자동 메일을 실행하지 않는다. worker는 Railway production만 허용한다.

## 검증

- 최종 로컬/CI 결과는 PR 본문에 기록한다. 조기 연장·만료 후 구매·체험 잔여 기간·중복 승인·worker 실패 복구 회귀 테스트를 추가했다.
- 구매 흐름 E2E는 KR/EN·320/768/1440/영어 라이트를 검사한다. CI의 PostgreSQL 17 통합 테스트가 동시 승인·중복 승인·환불 공백·만료 경계·기존 주문 호환을 검사한다.
- 로컬 PGlite에서 migration 2회·환불 공백·정확한 만료·체험 연결을 검증했다. 단일 연결 PGlite는 동시성 검증을 대체하지 않는다. 실제 운영 DB·거래·메일 수신·인앱 복귀는 별도 확인 대상이다.

## 근거

- [Toss 가상계좌 흐름](https://docs.tosspayments.com/guides/v2/payment-window/integration-virtual-account): WAITING_FOR_DEPOSIT는 입금 전 상태이며 입금 후 DONE으로 전환.
- [Toss 주문서형 SDK](https://docs.tosspayments.com/sdk/v2/js/payment-widget): 위젯 초기화·금액 지정과 실제 requestPayment 주문번호 전달은 분리 가능.
