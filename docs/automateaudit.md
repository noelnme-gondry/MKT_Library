# 사이트 전수 감사 (Automate Audit)

> **목적**: 사이트 전체를 반복 가능한 방식으로 감사한다. 이 문서는 ① 감사 프로토콜(어떻게 볼 것인가) ② 재실행 가능한 스윕 명령 ③ 실행 결과 로그를 한 파일에 담는다.
> **대상**: `v2-migration/` (운영 앱 SSOT) + `content/` 콘텐츠 + 공개 라우트 전체.
> **원칙**: 발견 항목은 **파일:줄 또는 실측 수치**를 근거로 남긴다. 근거 없는 인상 비평은 기록하지 않는다(§8 정직성).

---

## 0. 이 문서의 사용법

| 상황 | 할 일 |
|---|---|
| 정기 감사(월 1회 권장) | §3 스윕 전부 실행 → §5에 새 실행 로그 추가 → §6 백로그 갱신 |
| 기능 추가 후 | 해당 렌즈만 부분 실행 |
| 발견 항목 수정 완료 | §6 표의 상태를 `해결`로 바꾸고 근거 PR 번호 기재 |

**중요**: 이 문서는 감사 기록이지 실행 계획이 아니다. 실제 수정은 별도 PR로 하고, 여기엔 결과만 반영한다.

---

## 1. 감사 단위 — 표면 × 상태

도구 단위로 보면 이미 여러 번 감사했다(`full-tool-improvement-30-pass-audit.md` 등). 전수 조사는 단위를 바꾼다.

**단위 = 표면(surface) × 상태(state)**

버그와 이탈은 해피패스가 아니라 나머지 상태에 산다. 도구 1개의 실제 조합:

```
상태 5  (미업로드 · 데모 · 매핑 중 · 분석 완료 · 계산불가/컬럼부족)
× 테마 2 (다크 · 라이트)
× 로케일 2 (KR · EN)
× 뷰포트 2 (데스크톱 · 모바일)
= 40 조합
```

전부 보는 건 낭비다. **의도적 샘플링**:

- 전 도구 전수: `미업로드` · `계산불가` · `모바일 KR` (여기가 가장 자주 썩는다)
- 대표 3개 도구만 풀 매트릭스: 5-2(대시보드) · 5-18(가장 복잡) · 5-20(Aha)
- 콘텐츠 표면: 목록/상세 × KR/EN 4조합 + 검색 착지 시나리오

---

## 2. 렌즈 (L1~L7)

각 렌즈는 **판정 기준을 먼저 문장으로 고정**한다. "좀 아쉽다" 같은 판정은 재현이 안 되므로 금지.

### L1. 정보구조 · 유저 플로우
- 3대 진입(검색→글/용어 · 직접→홈 · 인앱→사이드바)별로 "첫 화면에서 다음 클릭이 자명한가"
- 막다른 길: 결과 화면에서 나가는 링크 0개인 표면
- 뒤로가기·새로고침 시 상태 유실(무주소 상태 화면 재발 여부)
- 첫 방문자가 도구를 고를 수 있는가(라벨만 보고)

### L2. 디자인 · UI 일관성
- 같은 의미의 요소가 표면마다 다른 모양인가(버튼 위계·카드·표·칩·빈 상태)
- `globals.css` 토큰 밖 하드코딩 색/여백 — **`getCssVar` 폴백은 정상 패턴, 순수 하드코딩만 드리프트로 집계**
- 다크/라이트 양쪽 대비(차트 범례·보조 텍스트는 상습 함정 §7)
- 모바일: 표 가로 스크롤 · sticky 겹침 · 터치 타깃
- 화면당 "가장 큰 것"이 실제로 가장 중요한 것인가

### L3. 카피 · 이해 가능성
- 표면당 설명 없는 전문용어 개수
- 결론이 평어 문장인가, 숫자만 던지는가
- 라벨이 질문 형태인가(`v2-migration/claude-ux.md` 원칙)
- KR/EN 의미 등가성 — 반쪽 번역 표면(§2.11)

### L4. 신뢰 · 정직성 (제품 생명선)
- 화면의 모든 숫자가 "어디서 나왔나" 추적 가능한가
- 관측 데이터에 인과 단정 카피가 있는가
- 추정 불가 상황에서 0/빈칸 대신 정직한 메시지가 뜨는가
- 프라이버시 주장과 실제 동작 일치(클라이언트 처리 · persist 대상 · GA4 전송 메타데이터)

### L5. 재사용 · 리텐션 루프
- 분석 종료 화면에 "다음 주에 다시 올 이유"가 명시되는가
- 재방문 마찰: 매번 CSV 재업로드 vs 저장 금지 원칙(§2.2)의 충돌을 어떻게 푸는가
- 결정 인박스·주간 검토 루프가 **끝까지 연결**되는가(진입점 → 기록 → 재방문 → 검토)
- 수익화 요소가 재방문 비용을 얼마나 올리는가

### L6. 시장 가치 · 포지셔닝
- 대안 대비 위치: 엑셀 · GA4 · 대행사 리포트 · Supermetrics류 · 무료 계산기
- "왜 이걸 쓰는가" 한 문장이 첫 화면에 있는가
- 검색 수요 대비 커버리지(GSC 쿼리 ↔ 발행 콘텐츠 매칭)
- 가치 포착 구조가 제품 신뢰와 상충하지 않는가

### L7. SEO · AAO · GEO ★신규
검색엔진과 AI 답변 양쪽에서 **인용 가능한 상태**인가. 기준은 `aao-geo-operating-model.md`를 따르며, 전용 스키마·`llms.txt` 같은 조작적 기법은 목표로 삼지 않는다.

**L7-a 기술 SEO**
- canonical · hreflang(ko/en/x-default) 전 공개 라우트 배선
- sitemap/RSS가 공개 범위 SSOT(`isRoutePublished` + `getAllPosts/getAllTerms`)에서 파생되는가, 초안·내부 라우트 제외되는가
- robots 허용 범위 · sitemap 고지
- 메타 제목/설명 **로케일별 잘림 임계**: KR 제목 ≤40자·설명 ≤90자 / EN 제목 ≤60자·설명 ≤160자
- OG/Twitter 카드가 글별로 생성되는가

**L7-b 구조화 데이터**
- BlogPosting · DefinedTerm · BreadcrumbList · FAQPage · SoftwareApplication · HowTo 적용 표면과 커버리지
- `author`가 조직인가 사람인가(E-E-A-T)
- `citation`이 본문 실제 인용과 일치하는가

**L7-c AAO/GEO (AI 답변 인용 가능성)**
- `answer`: 질문에 두 문장 안에 직접 답하는가, CTA·메타를 섞지 않았는가
- `conditions`: 답이 성립하는 데이터·기간·표본·플랫폼 조건이 **글별로 특정**되는가
- `reviewedAt`/`reviewer`: 실제 검토가 있었을 때만 노출되는가 (없으면 신뢰 신호 부재로 집계)
- `sources`: 외부 사실·플랫폼 정책 주장에 1차 출처가 붙는가
- 글 → 도구 → 분석의 양방향 연결이 레지스트리로 강제되는가

**L7-d 콘텐츠 자산 정합**
- KR/EN 짝 파일 완전성(고아 slug 0)
- 레지스트리 3종(`contentToolRegistry` · `blogSeo` · `localizedHref`) 정합
- 발행 글 ↔ 용어 상호 링크

### 횡단
성능(LCP·번들·차트 초기 렌더) · 접근성(h1 유일성·탭 시맨틱·포커스·alt) · 오류 복원력.

---

## 3. 재실행 가능한 스윕 (Automate)

아래 명령은 `v2-migration/`에서 실행한다. 수치가 바뀌면 §5에 새 실행 로그를 남긴다.

### S1. 콘텐츠 KR/EN 짝 정합 (L7-d)
```bash
for d in blog blog-en glossary glossary-en; do echo "$d: $(ls content/$d/*.md | grep -v '/_' | wc -l)"; done
comm -3 <(ls content/blog | grep -v '^_' | sed 's/.md//' | sort) \
        <(ls content/blog-en | grep -v '^_' | sed 's/.md//' | sort)
```
판정: 개수 일치 + comm 출력 0줄.

### S2. 메타 잘림 위험 (L7-a)
```bash
node -e "
const fs=require('fs');
function fm(p){const t=fs.readFileSync(p,'utf8');const m=t.match(/^---\n([\s\S]*?)\n---/);if(!m)return null;
 const o={};for(const l of m[1].split('\n')){const mm=l.match(/^(\w+):\s*(.*)\$/);if(mm)o[mm[1]]=mm[2].replace(/^[\"']|[\"']\$/g,'');}return o;}
for(const [dir,tl,dl] of [['content/blog',40,90],['content/blog-en',60,160]]){
  const fs2=fs.readdirSync(dir).filter(f=>f.endsWith('.md')&&!f.startsWith('_'));
  const t=[],d=[];
  for(const f of fs2){const o=fm(dir+'/'+f);if(!o)continue;
    if((o.title||'').length>tl)t.push(f+':'+o.title.length);
    if((o.description||'').length>dl)d.push(f+':'+o.description.length);}
  console.log(dir,'| title>'+tl+':',t.length,'| desc>'+dl+':',d.length);
  if(d.length)console.log('  ',d.join(' '));
}"
```
판정: 초과 0건.

### S3. AAO/GEO 편집 정보 커버리지 (L7-c)
```bash
node -e "
const fs=require('fs');const s=fs.readFileSync('src/lib/blogEditorial.js','utf8');
const posts=fs.readdirSync('content/blog').filter(f=>f.endsWith('.md')&&!f.startsWith('_')).map(f=>f.replace('.md',''));
function keys(name){const i=s.indexOf('const '+name);if(i<0)return[];const j=s.indexOf('\n};',i);
 return [...s.slice(i,j).matchAll(/\"([a-z0-9-]+)\":/g)].map(m=>m[1]);}
for(const n of ['KO_ANSWERS','EN_ANSWERS','CONDITION_GROUP_BY_SLUG'])
 console.log(n,'커버',keys(n).length,'/ 누락',posts.filter(p=>!keys(n).includes(p)).length);
let rv=0,src=0;
for(const p of posts){const t=fs.readFileSync('content/blog/'+p+'.md','utf8');
 if(/reviewedAt:/.test(t.split('---')[1]||''))rv++; if(/https:\/\//.test(t))src++;}
console.log('reviewedAt 보유:',rv,'/',posts.length,'| 외부 출처 인용 글:',src);"
```
판정: answers/conditions 누락 0 · reviewedAt·출처는 목표치 대비 비율로 추적.

### S4. 디자인 토큰 드리프트 (L2)
```bash
node -e "
const fs=require('fs'),path=require('path');
function walk(d,o=[]){for(const f of fs.readdirSync(d)){const p=path.join(d,f);
 fs.statSync(p).isDirectory()?walk(p,o):(/\.(jsx|js)\$/.test(f)&&!/\.test\./.test(f)&&o.push(p));}return o;}
let fb=0,drift=0;const by={};
for(const p of walk('src/components'))
 fs.readFileSync(p,'utf8').split('\n').forEach(l=>{const m=l.match(/#[0-9a-fA-F]{6}\b/g);if(!m)return;
  if(/getCssVar|var\(--/.test(l)){fb+=m.length;return;} drift+=m.length; by[p]=(by[p]||0)+m.length;});
console.log('토큰 폴백(정상):',fb,'| 실제 드리프트:',drift);
console.log(Object.entries(by).sort((a,b)=>b[1]-a[1]).slice(0,10).map(([f,n])=>'  '+n+'  '+f).join('\n'));"
```
판정: 드리프트 감소 추세. **`getCssVar(...) || "#hex"` 폴백은 위반 아님**(오탐 주의).

### S5. 정직성 어휘 (L4)
```bash
grep -rnoE "때문에|덕분에|증가시켰|감소시켰|효과가 있었" src/components --include=*.jsx
```
판정: 히트마다 문맥 확인. 코드 주석·"허위 인과 방지" 설명은 위반 아님.

### S6. 문서 드리프트 (횡단)
```bash
for f in $(grep -oE 'src/[a-zA-Z/.\[]+\.(jsx|js)' ARCHITECTURE.md | sort -u); do
  [ -f "$f" ] || echo "MISS $f"; done
echo "routeMap slug: $(grep -c 'slug:' src/lib/routeMap.js)"
```
판정: MISS 0 · ARCHITECTURE 기재 라우트 수와 실제 일치.

### S7. 배포 게이트
```bash
npm run test:all && npm run lint && npx next build
```

---

## 4. 발견 항목 기록 스키마

```
ID · 표면 · 렌즈 · 증상(1문장) · 증거등급 · 근거(파일:줄/수치) · 심각도 · 난이도 · 제안
```

**증거등급**: `측정`(스윕·테스트 수치) · `관찰`(코드 직접 확인) · `추론`(판단, 실데이터 없음)
**심각도**: `P0` 거짓 숫자·프라이버시 위반·화면 깨짐 / `P1` 이탈·재방문 차단 / `P2` 이해비용·일관성 / `P3` 다듬기

---

## 5. 실행 로그

### 2026-08-01 · 1차 (코드·콘텐츠 정적 감사)

**범위**: S1~S6 전 스윕 + S7 테스트 스위트 + 라우트/컴포넌트 코드 확인.
**미수행**: 라이브 브라우저 확인, GA4/GSC 실데이터, 성능 측정(LCP·번들), 실사용자 테스트, `next build`. → **L5·L6 판정은 `추론` 등급**이며 측정으로 승격하려면 GA4/GSC 접근이 필요하다.

**기준 커밋**: `main` #567(Operator Desk 시각 시스템 전면 개편) 반영 후. 수치를 재현하려면 같은 시점 이후에서 §3을 실행한다.

**S7 결과**: `npm run test:all` → **164 파일 중 1 실패 / 1,117 테스트 중 3 실패 · 1,113 통과 · 1 스킵**, 총 227초. 실패 3건은 모두 `attributedForecastLiveMath.test.js`의 **타임아웃**(46.8s·84.9s·7.2s vs 제한 30s·30s·5s)이며 단정 실패가 아니다.
→ **후속 확인 결과 이 실패는 감사 실행 환경(느린 컨테이너) 고유 현상이다.** GitHub Actions(`ubuntu-latest`)의 `main` 최근 8회 실행이 전부 success이며 같은 명령(`npm run test:all`)을 돈다. **배포 게이트는 초록이다.** F12는 "게이트 실패"가 아니라 "실행 환경 민감도" 관찰로만 남긴다.

#### 5.1 통과 항목 (기록해야 신뢰가 쌓인다)

| 렌즈 | 확인 내용 | 근거 |
|---|---|---|
| L7-d | KR/EN 짝 완전 — blog 32/32, glossary 25/25, 고아 slug 0 | S1 |
| L7-c | `answer` KR 32/32 · EN 32/32, 조건 그룹 매핑 32/32 누락 0 | S3 |
| L7-a | hreflang ko/en/x-default 배선, blog 상세는 EN 짝 존재 시에만 en 방출 | `src/app/blog/[slug]/page.js:22-23`, `src/lib/routeMap.js:138-146` |
| L7-a | sitemap이 EN 블로그 목록·상세 포함 | `src/app/sitemap.js:112-117` |
| L7-a | robots 전체 허용 + sitemap/host 고지 | `src/app/robots.js` |
| L7-b | JSON-LD 6종 이상 운용(BlogPosting·DefinedTerm·FAQPage·BreadcrumbList·SoftwareApplication·HowTo) | 코드 전수 grep |
| L4 | 인과 단정 어휘 스윕 클린 — 히트 2건 모두 코드 주석·"허위 그랜저-인과 방지" 설명 | S5, `marketingResponseModel.jsx:1918` |
| L4 | 결정 저장이 **opt-in + sanitize + 마이그레이션 방어**. 원본 CSV·매핑·필터는 persist 불가 | `src/store/useDataStore.js:332-357` |
| L5 | 결정 검토함 진입점 3곳(Header·Footer·DecisionReview) KR/EN 양쪽 | `Header.jsx:201`, `Footer.jsx:22`, `ds/DecisionReview.jsx:356` |
| L1 | 랜딩이 도구명이 아니라 **업무 질문 3개**로 진입 + 진단 라우트 별도 제공 | `LandingPage.jsx:11-40` |
| L2 | `getCssVar` 폴백 패턴 66건 — 토큰 우선 규약이 실제로 지켜지는 영역 존재 | S4 |

#### 5.2 발견 항목

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 | 난이도 |
|---|---|---|---|---|---|---|
| F1 | L7-c | 발행 글 **32편 전부 `reviewedAt`/`reviewer` 없음** → 검토 책임 신호가 화면·구조화 데이터 어디에도 안 뜬다. AI 답변·검색 모두 신선도/책임 근거를 못 읽는다 | 측정 | S3 (0/32) | **P1** | S |
| F2 | L7-c | **외부 1차 출처를 인용한 글 3/32** → `BlogPosting.citation`이 대부분 비어 AI 답변이 인용할 근거가 약하다. 플랫폼 정책·수치 주장 글에 출처가 없다 | 측정 | S3 | **P1** | M |
| F3 | L7-c | 적용 조건이 **7개 그룹 문구를 32편이 공유**(platform 9편·measurement 9편·causal 6편 등) → 글별 특정 조건이 아니라 범용 문구. AAO가 요구하는 "조건 명확성"에 미달 | 측정 | `blogEditorial.js` CONDITION_GROUP_BY_SLUG | P2 | M |
| F4 | L7-a | **EN 메타 설명 15/32이 160자 초과**(최대 246자), **EN 제목 15/32이 60자 초과**(최대 85자) → SERP 잘림. KR 기준(40/80)을 EN에 그대로 쓴 스키마가 원인 | 측정 | S2 | **P1** | S |
| F5 | L7-a | KR 설명 5건이 90자 초과(최대 128자) | 측정 | S2 | P3 | S |
| F6 | L7-b | FAQ 보유 글 **8/32** → FAQPage 구조화 데이터 커버리지 25%. 질문형 검색 대응 표면이 좁다 | 측정 | frontmatter grep | P2 | M |
| F7 | L7-b | `BlogPosting.author`가 발행 조직(publisher) — **Person 저자 없음**. 실무 경험 기반 콘텐츠인데 사람 저자 신호가 없다 | 관찰 | `blog/[slug]/page.js:111` | P2 | S |
| F8 | L2 | 토큰 없이 하드코딩된 색 **317건**(MarketingResponse.jsx 104 · marketingResponseModel 33 · AhaMoment 21 …) → 다크/라이트 전환 시 깨질 후보. §7의 상습 함정 영역. #567 시각 시스템 개편 후에도 323→317로 거의 그대로 = 개편이 도구 내부까지 닿지 않았다 | 측정 | S4 | **P1** | L |
| F9 | 횡단 | `MarketingResponse.jsx` 5,957줄 + `marketingResponseModel.jsx` 4,892줄 = **단일 도구 10,849줄**. F8 드리프트의 42%가 여기 집중 | 측정 | `wc -l` | P2 | L |
| F10 | 횡단 | **문서 드리프트**: `ARCHITECTURE.md`·`CLAUDE.md §12.26`이 실존하지 않는 `AdInterstitial.jsx`·`AdFreeInit.jsx`를 라이브로 기술. 라우트도 문서 23개 vs `routeMap` 37개, 9-x 도구 문서 2개 vs 실제 5개 | 측정 | S6 | **P1** | S |
| F11 | L6 | 수익 요소(AdSense)가 **`/blog`·`/en/blog` 레이아웃에만** 로드 → 제품 가치의 핵심인 도구 페이지는 수익화 0. 트래픽 가치와 제품 가치가 분리돼 있다 | 관찰 | `src/app/blog/layout.js:13` | P2 | — |
| F12 | 횡단 | **골든 3건이 느린 환경에서 타임아웃**(`attributedForecastLiveMath.test.js`, 46.8s·84.9s·7.2s vs 30s·30s·5s). **CI·main은 초록이므로 결함이 아니다.** 다만 개별 테스트가 45~85초 걸리는 구조라 감사·로컬 환경에 따라 결과가 갈리고, 전체 227초는 반복 감사에 부담이다 | 측정 | S7 + main CI 8회 연속 success | P3 | M |

#### 5.3 F10 상세 (하네스 정합)

`CLAUDE.md §12.26`은 "분석하기 → 전면 광고 → 카운트다운" 흐름을 현재 동작으로 기술하지만 해당 컴포넌트가 코드베이스에 없다. 하네스 문서가 현실과 어긋나면 이후 모든 작업이 잘못된 전제 위에서 시작된다. **§15 자가 업데이트 대상**.

---

## 6. 우선순위 백로그

우선순위 = (영향 × 심각도) ÷ 난이도. 상위만 실행 후보로 두고 나머지는 보류 registry로 남긴다(전부 고치려다 아무것도 안 끝나는 게 감사 문서의 사망 원인).

| 순위 | 항목 | 이유 | 상태 |
|---|---|---|---|
| 1 | **F10** 문서 드리프트 정정 | 다른 모든 작업의 전제. 난이도 S | 미착수 |
| 2 | **F4** EN 메타 임계 분리(KR 40/80 · EN 60/160) + 초과 15건 수정 | SERP 노출 직접 손실, 난이도 S | 미착수 |
| 3 | **F1** 검토일·검토자 운영 시작 | AAO/GEO 신뢰 신호 0 → 있음. 단 **실제 검토했을 때만** 기재(§8) | 미착수 |
| 4 | **F2** 출처 인용 정책 — 플랫폼 정책·수치 주장 글 우선 | AI 답변 인용 가능성 직결 | 미착수 |
| 5 | **F8** 토큰 드리프트 — MarketingResponse부터 | 테마 깨짐 예방, 난이도 L이라 분할 필요 | 미착수 |
| 6 | **F3** 글별 조건 특정화 | F1·F2 이후 | 보류 |
| 7 | **F6/F7** FAQ 확대·Person 저자 | 중기 | 보류 |
| — | **F11** 수익 구조 | 데이터(수익·이탈) 없이 결정 불가 → GA/AdSense 실적 확인 후 판단 | 판단 보류 |
| — | **F9** 대형 컴포넌트 분할 | 기능 변경 없는 리스크. F8 작업과 묶을 때만 | 보류 |
| — | **F12** 골든 타임아웃 | CI·main 초록 확인 → 결함 아님. 반복 감사 편의를 위한 시간 예산 조정은 선택 사항 | 조치 불필요 |

---

## 7. 측정으로 승격해야 할 것 (현재 `추론` 등급)

정적 감사로는 답할 수 없고 실데이터가 필요한 질문:

| 질문 | 필요한 데이터 | 렌즈 |
|---|---|---|
| 사람들이 실제로 어디서 이탈하는가 | GA4 퍼널 이벤트 | L1·L5 |
| 재방문율과 결정 검토함 사용률 | GA4 재방문·이벤트 | L5 |
| 어떤 검색어로 들어와서 도구까지 가는가 | GSC 쿼리 + `ContentActionPanel` 전환 | L6·L7 |
| AI 답변에서 인용·언급되는가 | GSC 생성형 AI 성과 + 고정 질의 세트 수동 확인(`aao-geo-query-set.md`) | L7-c |
| 광고 수익 대비 이탈 비용 | AdSense + GA4 | L6 |

이 다섯 개를 채우기 전에는 리텐션·시장가치 항목에 대해 "이렇게 하면 좋아진다"고 단정하지 않는다.

---

## 8. 이번 감사에서 하지 않은 것

- 수학 엔진 수정 (§2.1 — 골든 불변, 렌더층만)
- 기존 감사 문서 재탕 — 중복 발견은 여기 기록하지 않고 원 문서를 참조
- 증거 없는 리텐션 처방
- 새 도구 추가 제안

---

*근거 수치는 2026-08-01 기준. 재실행 시 §3 스윕을 돌리고 §5에 새 로그를 추가한다.*

---

## Round 1 — Codex 응답 (2026-08-02)

> **스태킹 원칙**: 위 감사 원문과 당시 수치·판정은 수정하거나 삭제하지 않는다. 이후 검증, 반론, 수정 결과는 `Round N`으로 문서 끝에만 추가한다.

**검토 기준**: 감사 브랜치 `4789296f`, `main` `4c9974ab` 기준으로 원문 주장과 실제 런타임 SSOT·테스트를 다시 대조했다.

### R1-1. 판정

| 원 항목 | 분류 | 재검증 결과 |
|---|---|---|
| F10 문서 드리프트 | **즉시 수정** | 사실이다. 삭제된 `AdInterstitial.jsx`·`AdFreeInit.jsx`가 라이브 컴포넌트로 남아 있었고, sitemap URL 수와 5-18·9-x 진입점 문서가 현재 `routeMap`과 달랐다 |
| F4/F5 메타 길이 | **일부 오탐 + 실제 문제 수정** | 원 S2는 원고 frontmatter를 측정하지만 배포 메타 SSOT는 `blogSeo.js`다. 실제 배포값은 수정 전 KR 제목/설명 초과 0/0, EN 제목/설명 초과 **17/0**이었다. 설명 초과 주장은 오탐이고 EN 제목 초과만 실제 문제였다 |
| F1 검토자·검토일 / F7 Person 저자 | **사용자·운영 판단 필요** | 노출 코드와 구조는 이미 있다. 실제 검토 없이 32편에 이름·날짜를 채우면 거짓 신뢰 신호가 되므로 자동 수정하지 않는다. 검토 책임자 표기명과 검토 절차 확정이 먼저다 |
| F2 출처 / F3 조건 / F6 FAQ | **순차 실행** | 플랫폼 정책·수치 주장이 많은 원고부터 1차 출처, 글별 조건, 실제 검색 질문을 한 원고 단위로 함께 검토해야 한다. 개수만 채우는 출처·FAQ 추가는 하지 않는다 |
| F8 색상 / F9 대형 컴포넌트 | **방향은 유효, 일괄수정은 비권장** | 317개 hex에는 의미 색상·차트 팔레트가 섞여 있다. 전부 토큰으로 기계 치환하거나 10,849줄 분할까지 동시에 하면 회귀 위험이 더 크다. UI 표면과 데이터 팔레트를 분류한 뒤 도구 단위로 수정한다 |
| F11 도구 페이지 수익화 | **사용자 판단 필요** | 수익·이탈·AdSense 정책 데이터 없이 광고를 추가하지 않는다 |
| F12 타임아웃 | **조치 없음** | 동일 게이트의 GitHub Actions가 안정적으로 통과하므로 제품 결함으로 승격하지 않는다 |

### R1-2. 같은 PR에 반영한 수정

1. **실제 EN 메타 제목 17건 압축**
   - `src/lib/blogSeo.js`의 최종 EN 제목을 60자 이하로 줄였다.
   - 고의도 검색어 계약(`marketing budget allocation` 등)은 유지했다.
   - `blogSeo.test.js`에 KR 40/80, EN 60/160 제목·설명 상한을 함께 검증하는 회귀 게이트를 추가했다.

2. **하네스·코드맵 드리프트 수정**
   - `ARCHITECTURE.md`에서 삭제된 광고 컴포넌트를 제거하고 sitemap 고정 개수 표기를 SSOT 파생 규칙으로 바꿨다.
   - 5-18의 세부 진입 라우트 4개와 9-3·9-6·9-7을 코드맵에 추가했다.
   - `CLAUDE.md §12.26`을 현재 상태인 “전면광고 폐기, `requestAd(cb)` 즉시 실행 호환 래퍼만 유지”로 정정했다.

3. **감사 개선 방향 확정**
   - 다음 라운드부터 메타 길이는 원고가 아니라 `getBlogSeo()` 최종 출력으로 재검증한다.
   - 원 감사 명령과 기록은 비교 가능한 과거 근거이므로 그대로 유지한다.

### R1-3. 검증

- 실제 메타 재측정: KR 제목/설명 초과 `0/0`, EN 제목/설명 초과 `0/0`
- 콘텐츠 짝: blog `32/32`, glossary `25/25`, 고아 slug `0`
- 문서 `src/*` 경로 누락: `0`
- `npm run test:all`: **166파일, 1,126개 통과, 1개 스킵**
- `npm run lint`: 통과
- `npm run build`: 통과, 정적 페이지 **220개** 생성

### R1-4. 남은 사용자 결정

| 결정 | 필요한 답 |
|---|---|
| 검토 신뢰 신호 | 공개 표기할 실제 검토 책임자 이름 또는 조직명, 그리고 “검토 완료”의 최소 절차 |
| 도구 페이지 수익화 | 광고를 실험할지 여부. 실행 전 GA4 이탈·재방문과 AdSense 수익 기준을 먼저 정해야 함 |

**반영 커밋**: `032a8f5e` (`fix: 감사 오탐과 메타 드리프트 정리`)
**후속 검토 예약**: 2026-08-02 03:30부터 10:30까지 매시간 총 8회. 각 회차는 새 감사 내용이 있을 때만 검증·수정·커밋하고 `Round N`을 이어 붙인다.

---

## Round 2 — Claude 감사 (2026-08-02 03:00 KST)

**동기화**: 감사 브랜치 `5b226f7` = origin, `origin/main` `4c9974ab`와 차이 0.
**이 라운드 구간 코드 변경**: R1(`5b226f7`) 이후 커밋 **0건**. Codex 회차는 :30 일정이므로 이 구간에는 반영된 코드 변경이 없다.
**신규 기능 딥다이브**: **대상 없음**(신규 기능·라우트·컴포넌트 0건). 다음 라운드에 신규 기능이 들어오면 상태 매트릭스 전수 + L1~L7 정밀 감사를 적용한다.

### R2-1. R1 조치 독립 검증 — 통과

| 항목 | R1 조치 | R2 재측정 |
|---|---|---|
| 실제 배포 메타(S2 교정판) | EN 제목 17건 압축 | **ko 제목/설명 0/0 · en 제목/설명 0/0** — 반영 확인 |
| 문서 `src/*` 경로(S6) | 삭제 컴포넌트 제거 | **MISS 0** — 반영 확인 |
| 콘텐츠 짝(S1) | — | blog 32/32 · glossary 25/25 · 고아 0 (불변) |
| 토큰 드리프트(S4) | 의도적 보류 | 폴백 63 · 드리프트 **317** (불변, 보류 판단대로) |
| routeMap slug(S6) | SSOT 파생 표기로 변경 | 37 (불변) |

### R2-2. 신규 발견

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F13 | 횡단 | **5-6 → 9-6 통합이 문서에 미반영.** `routeMap.js:20`은 "5-6은 9-6으로 통합됨"을 주석으로 명시하고 실제 id 목록에도 5-6이 없다. 그런데 `ARCHITECTURE.md:49`는 `/tools/creative-analysis` 를 `5-6`으로, `CLAUDE.md:73`은 `5-6 소재 분석`으로 여전히 라이브 도구처럼 기재한다. **F10의 잔여분** | 측정 | `routeMap.js:20`, `ARCHITECTURE.md:49`, `CLAUDE.md:73` | **P1** |
| F14 | L1 | **콘텐츠 3개 도구(9-2·9-3·9-7)에 "다음 분석" 연결이 없다.** `CONNECTED_TOOLS`·`NEXT_TOOL_IDS` 어디에도 키가 없어 `getNextTools()`가 빈 배열을 반환한다. 분석을 마쳐도 다음 행동 제안이 뜨지 않는 **막다른 길**이다. 같은 콘텐츠 계열인 9-1·9-6은 연결이 있어 **비대칭** | 측정 | `toolConnections.js:4-45, 85-118` | **P1** |
| F15 | L2 | **`ds/DataTable` 채택 1/15.** 도구·대시보드 컴포넌트 14개가 직접 `<table>`을 쓴다(AhaMomentFinder·CreativeAnalyzer·MarketingResponse·MarketingEfficiency·AbTestHoldout·CampaignPvm·BudgetAllocation·ContentElementAnalyzer·Ltv/Cohort/Segment/Anomaly/FunnelTab). `CLAUDE.md §12.21`이 신규 분석 도구에 DataTable을 명시하는데 실사용은 1개 | 측정 | 도구·대시보드 전수 | P2 |
| F16 | L3 | **한국어 UI 문자열 안에 미번역 전문용어가 그대로 노출.** 코드 식별자가 아니라 `tx()` 표시 문자열 내부다 — 예: "증액 후 지속 주간 노출이 관측 **adstock** 범위 밖"(`MarketingResponse.jsx:4291`), "상위 그룹 총량의 **adstock carryover** 비중 배분"(`:4703`), "① 채널별 광고 여운·**포화**"(`:4967`) | 관찰 | `MarketingResponse.jsx:4291·4703·4967` | P2 |

### R2-3. 측정 한계 — 수치 주장하지 않음

R1에서 F4/F5가 **측정 대상 레이어를 틀려** 오탐이 됐다. 같은 계열 실수를 반복하지 않기 위해 아래 두 항목은 관찰만 남기고 **수치로 판정하지 않는다.**

- **L3 전문용어 총량**: grep은 변수·함수 이름까지 세므로 "노출량" 지표가 될 수 없다. F16은 실제 `tx()` 문자열을 확인한 3건만 근거로 삼았고 총량은 미측정이다.
- **탭 접근성**: `role="tablist"`가 5개 파일에서 확인되나 "탭 UI가 총 몇 개인지"를 grep으로 셀 수 없어 커버리지 판정이 불가하다.

### R2-4. 스윕 개선 제안 (§3 보강 후보)

| 제안 | 이유 |
|---|---|
| S6에 **route id 대조** 추가 | 현 S6은 `src/*` 파일 존재만 검사한다. F13처럼 **문서가 죽은 route id를 라이브로 기재**하는 드리프트를 구조적으로 못 잡는다. `routeMap`의 id 집합과 문서 표기 id를 비교해야 한다 |
| S8 **UI 문자열 전문용어** | 코드 식별자를 제외하고 `tr()`/`tx()` 인자 문자열만 추출해 용어 노출을 측정 |
| S9 **탭 시맨틱 커버리지** | 탭처럼 동작하는 UI를 식별해 `tablist/tab/tabpanel` 적용률 측정 |

### R2-5. 아이디에이션

> **아래는 전부 [아이디에이션]이다.** 위 발견(F)은 측정·관찰된 사실이지만 여기 항목은 **검증되지 않은 제안**이며, 각각 **가정**과 **검증 방법**을 병기한다. 섞어 읽지 말 것.
> 판단 기준 두 개 — **첫인상**(딱 봤을 때 쓰고 싶은가) · **재사용**(쓰고 나서 또 오고 싶은가). 제품 원칙(원본 CSV 저장 금지·거짓 숫자 금지·가입 강제 금지) 위반 아이디어는 폐기 사유와 함께 남긴다.

**첫인상 — "딱 봤을 때 쓰고 싶은가"**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I1 | **업로드 전에 "내 상황"을 고르게 한다** — 데모를 업종·규모별 프리셋(앱커머스/게임/구독/리드젠)으로 나눠 자기 상황에 투영된 판단 화면을 먼저 보여준다 | 이탈 원인은 "못 믿어서"가 아니라 "내 얘기인지 몰라서"다 | 프리셋별 진입 → 업로드 전환율(GA4). 프리셋을 늘려도 전환이 안 움직이면 가정이 틀린 것 |
| I2 | **CSV 없이 3필드로 첫 답** — 지출·전환·기간만으로 판단 1개를 준다. 이미 있는 `/calculator`를 도구 진입 앞단에 배치해 업로드 마찰을 뒤로 민다 | 최대 이탈 지점은 CSV 준비다 | 계산기 → 도구 진입률. 계산기에서만 끝나면 가정이 틀렸거나 계산기가 종착지가 된 것 |
| I3 | **파일을 넣자마자 "무엇을 알 수 있는지" 먼저** — 매핑 화면 전에 "이 파일로 A·B 가능, C는 컬럼 부족"을 전면에. `lib/analysis-router/evaluateEligibility.js`가 이미 그 판정을 한다 | 매핑에서 이탈하는 이유는 "다 채우면 뭐가 나오는지 몰라서"다 | 업로드 → 분석 실행 전환율 |
| I4 | **보고용 1장** — 결과를 슬랙·주간보고에 그대로 붙일 수 있는 1장 포맷. 마케터가 상사에게 공유하는 순간이 가장 강한 획득 채널이다 | 마케터의 실제 산출물은 "분석"이 아니라 "보고"다 | 다운로드 후 재방문·신규 유입 상관. `DownloadHub`의 PNG와는 별개 포맷 |

**재사용 — "쓰고 나서 또 오고 싶은가"**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I5 | **지난주 내 판단이 맞았는지 채점** — 결정 인박스 기록을 이번 주 데이터로 대조해 "맞음/빗나감/판단 불가"를 보여준다 | 마케터는 분석보다 "내 판단이 옳았나"를 더 궁금해한다 | 결정 기록 사용자의 4주 재방문율 vs 미사용자 |
| I6 | **같은 CSV 재업로드 시 자동 diff** — "지난번 대비 무엇이 변했나"를 먼저. 원본은 저장하지 않고 **요약 지표만** opt-in 저장(`decisionRecords`가 이미 그 구조) | 주간 운영자는 절대값보다 델타를 본다 | 재업로드 비율·diff 뷰 체류 |
| I7 | **가입 없는 리마인더** — 이메일 수집 대신 캘린더 `.ics` 또는 브라우저 알림으로 "다음 주 검토"를 건다 | 재방문이 안 되는 건 의지가 아니라 트리거 부재다 | 리마인더 설정자의 재방문율 |
| I8 | **재방문자용 초압축 뷰** — 매주 오는 사람에게 9탭 대시보드는 과하다. "이번 주 바뀐 것만" 한 화면 | 신규와 재방문의 needs가 다르다 | 재방문자 첫 화면 이탈률 |
| I11 | **F14를 기회로** — 콘텐츠 도구(9-x)의 막다른 길을 메울 때 단순 연결이 아니라 **콘텐츠 → 퍼포먼스 교차 제안**으로 설계한다("이 콘텐츠가 유입은 늘렸는데, 광고 예산은 어디로?") | 콘텐츠 분석 사용자도 결국 광고 판단을 한다 | 9-x → 5-x 이동률. 낮으면 두 사용자군이 실제로 분리된 것 |

**시장 가치 · 포지셔닝**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I9 | **"도구 모음"이 아니라 "주간 운영 리듬"으로 일관 포지셔닝** — 랜딩 eyebrow(`WEEKLY DECISION SYSTEM`)는 이미 그 방향인데 사이드바·제품 내부는 여전히 도구 나열이다 | 경쟁 우위는 개별 분석 성능이 아니라 매주 돌아가는 루프다 | 리텐션 코호트 |
| I10 | **한국 시장 웨지** — 네이버·카카오 채널과 국내 매체 리포트 포맷 프리셋. 타겟이 KR 시니어 마케터인데 글로벌 도구가 잘 안 하는 영역이다 | 국내 매체 데이터 정합이 실무의 실제 통증이다 | GSC 국내 매체 쿼리 유입, 해당 프리셋 사용률 |
| I12 | **F16을 신뢰 자산으로** — 전문용어를 지우는 대신 **용어에 hover/탭하면 용어사전 정의가 뜨게** 한다. 이미 25개 용어사전이 있고 SEO 자산이다. 초보는 이해하고 시니어는 정확한 용어를 유지한다 | 용어 제거는 시니어에게 정보 손실이다. 문제는 용어가 아니라 설명 부재다 | 툴팁 오픈율, 용어사전 유입 |

**폐기**

| 아이디어 | 폐기 사유 |
|---|---|
| 재방문율을 올리려고 업로드한 CSV를 서버에 저장해 다음 방문에 자동 로드 | **CLAUDE.md §2.2 위반** — 사용자 CSV는 브라우저 메모리에만. 리텐션을 위해 제품의 핵심 신뢰 약속을 깨지 않는다. I6의 "요약 지표만 opt-in 저장"이 원칙을 지키는 대안이다 |
| 결과 전체 열람에 이메일 입력 요구(리드 수집) | 가입 없음이 랜딩의 명시 약속(`가입 없음 · 모든 분석 무료`)이라 카피와 모순된다. 동작하지 않는 약속은 실질 버그(CLAUDE.md §8) |

### R2-6. 다음 라운드로 넘기는 것

- F13·F14는 난이도 S다. 코딩 작업 쪽에서 처리 가능한지 R3에서 확인한다.
- F15·F16은 표면 단위 순차 작업이라 일괄 수정하지 않는다(R1의 F8 판단과 같은 이유).
- L5·L6은 이번 라운드도 GA4/GSC가 없어 `추론` 등급 그대로다. 정적 감사를 9번 반복해도 이 축은 채워지지 않는다.

---

## Round 3 — Claude 감사 (2026-08-02 04:00 KST)

**동기화**: 감사 브랜치 `a510d6e` = origin, `origin/main`과 차이 0.
**이 라운드 구간 코드 변경**: R2(`a510d6e`) 이후 커밋 **0건**. 2라운드 연속 코드 변경이 없다.
**신규 기능 딥다이브**: **대상 없음**.
**스윕 수치**: S1 blog 32/32 · glossary 25/25 · 고아 0 / S4 폴백 63 · 드리프트 **317** / S6 MISS 0 · slug 37 — **R2와 전부 동일**(변화 없음).

코드 변화가 없으므로 이 라운드는 **아직 실측하지 않은 렌즈**를 판다: L5 리텐션 루프가 실제로 닫히는지, L7-d 상호 링크 완전성.

### R3-1. L5 리텐션 루프 폐쇄 추적 (신규 실측)

R1은 "결정 검토함 진입점 3곳"까지만 확인했다. 이번엔 **루프가 실제로 닫히는지** — 도구 결과에서 결정이 기록되는 경로가 있는지 — 를 봤다.

구조는 제대로 돼 있다: `ds/ResultActionCard`가 `DecisionReview`를 내장하고(`decisionReview` 기본 true), `decisionPrefill`로 도구가 만든 결론·행동·기준값을 미리 채운다. `addDecisionRecord`는 store에 있고 opt-in persist를 통과한다.

| 도구 | ResultActionCard | decisionPrefill | 결정 기록 진입점 |
|---|---|---|---|
| 5-2 대시보드 | 1 | 1 | O |
| 5-4 실험 | 1 | 1 | O |
| 5-20 Aha | 1 | 3 | O |
| 5-3 예산배분 | 1 | 1 | O |
| 5-21 PVM | 1 | 1 | O |
| 9-1 요소분석 | 1 | 3 | O |
| 9-6 소재/신선도 | 1 | 1 | O |
| 5-23 증분 | 2 | 6 | O |
| 5-22 포화도 | 1 | 1 | O |
| **5-18 마케팅 반응** | 1 | **0** | **X** (`decisionReview={false}`) |

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F17 | L5 | **5-18(마케팅 반응·MMM·회귀예측)만 리텐션 루프 밖에 있다.** 유일한 `ResultActionCard`가 단계 이동 카드로 쓰이며 `decisionReview={false}`라, 이 도구에서는 결정 기록 진입점이 없고 `decisionPrefill`도 0이다. 단계 카드에서의 비활성화 자체는 의도일 수 있으나, **결과적으로 예산·기여 판단에 가장 직결되는 도구의 결론이 결정 인박스로 흐르지 않는다** | 측정 | `MarketingResponse.jsx:3780`, 도구 10개 전수 대조 | **P1** |

### R3-2. L7-d 상호 링크 완전성 (신규 실측)

R1이 판정 기준으로만 적고 재지 않았던 항목이다.

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F18 | L7-d | **글 → 용어 연결 31/32** — `RELATED_GLOSSARY`에 `adjust-vs-appsflyer` 누락. **용어 → 글 연결 23/25** — `deep-link`·`ecpi`에 `relatedPosts` 없음. 검색 착지 후 다음 표면으로 나가는 경로가 이 3개 문서에서만 끊긴다 | 측정 | `contentToolRegistry.js`, `content/glossary/*` | P2 |

누락된 `adjust-vs-appsflyer`는 **MMP 비교글**이다. 비교 검색어는 의사결정 직전 트래픽인데 다음 표면 연결이 없다(→ I16).

### R3-3. 변화 없음 기록

S1·S4·S6 수치가 R2와 완전히 동일하다. F8(토큰 드리프트 317)·F13~F16은 미착수 상태 그대로이며, 이는 R1에서 합의한 보류 판단(F8/F9 일괄수정 비권장)과 어긋나지 않는다. **2라운드 연속 코드 변경이 없다는 사실 자체를 기록으로 남긴다** — 감사만 반복되고 반영이 없으면 라운드의 가치가 떨어진다.

### R3-4. 아이디에이션

> **[아이디에이션]** — 아래는 검증되지 않은 제안이다. 위 발견(F)과 섞어 읽지 말 것. 각 항목에 가정·검증 방법을 병기한다.

**재사용 — "쓰고 나서 또 오고 싶은가"**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I13 | **5-18을 루프의 정점으로** — F17을 단순히 "카드 하나 켜기"로 끝내지 않는다. 예측 도구는 **채점이 가장 자연스러운 도구**다. "지난주 예측 vs 이번주 실제"를 결정 인박스에서 바로 대조해주면 재방문 이유가 제품 논리상 가장 강해진다 | 예측을 본 사람은 그 예측이 맞았는지 확인하러 돌아온다 | 5-18 사용자의 4주 재방문율, 예측-실제 대조 뷰 조회율 |
| I14 | **결정 프리필을 "문장"이 아니라 "채점 가능한 기준값"으로** — 지금 prefill은 결론·행동·기준값 제안이다. 여기서 기준값을 **다음 주 데이터로 자동 판정 가능한 형태**(지표·방향·임계)로 고정하면 I5(판단 채점)가 수동 입력 없이 성립한다 | 사람은 자기 판단을 다시 입력하지 않는다. 자동 채점만이 루프를 닫는다 | 기록된 결정 중 자동 판정 가능 비율 |
| I15 | **검색 → 용어 → 도구 3단 경로 완성** — F18의 끊긴 링크를 메우되 단순 연결이 아니라 용어 페이지마다 "이 개념을 내 데이터로 확인하기" 버튼을 표준화한다 | 용어 검색자는 개념 확인 후 자기 데이터로 넘어갈 의사가 있다 | 용어 → 도구 이동률, 이동 후 분석 실행률 |

**첫인상 — "딱 봤을 때 쓰고 싶은가"**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I16 | **비교글을 전환 표면으로** — `adjust-vs-appsflyer` 같은 "A vs B" 글은 **의사결정 직전 트래픽**이다. 여기에 "두 도구 데이터가 왜 다른지 내 CSV로 확인" 같은 도구 연결을 붙이면 전환 밀도가 가장 높을 수 있다. 마침 F18에서 이 글만 도구·용어 연결이 비어 있다 | 비교 검색자는 정보 수집이 아니라 결정 단계에 있다 | 비교글 → 도구 진입률 vs 정의형 글의 진입률 |
| I17 | **"이 화면은 3분이면 끝난다"를 명시** — 마케터의 첫 저항은 난이도가 아니라 **소요 시간 불확실성**이다. 각 도구 진입부에 필요한 컬럼 수·예상 소요를 숫자로 박는다 | 이탈은 "어려워 보여서"가 아니라 "얼마나 걸릴지 몰라서"다 | 진입부 이탈률 변화 |

**폐기**

| 아이디어 | 폐기 사유 |
|---|---|
| 결정 기록을 기본 on으로 바꿔 재방문율을 높인다 | 현재 opt-in + sanitize 구조는 프라이버시 약속을 지키는 설계다(`useDataStore.js:332-357`). 기본 on은 사용자가 동의하지 않은 저장이 되어 §2.2 취지에 어긋난다. 필요한 건 기본값 변경이 아니라 **저장의 가치를 먼저 보여주는 것**(I14) |

### R3-5. 다음 라운드로 넘기는 것

- F17은 난이도 S(카드 활성화)이지만 제대로 하려면 I13·I14와 함께 설계해야 값이 난다.
- F18은 난이도 S. 3개 문서 연결만 채우면 된다.
- 3라운드 연속 코드 변경이 없으면 R4에서 "감사 대비 반영 지연"을 별도 항목으로 기록한다.

---

## Round 4 — Claude 감사 (2026-08-02 05:00 KST)

**동기화**: 감사 브랜치 `b050edc` = origin, `origin/main`과 차이 0.
**이 라운드 구간 코드 변경**: R3(`b050edc`) 이후 커밋 **0건**. **3라운드 연속**이다.
**신규 기능 딥다이브**: 대상 없음.
**스윕 수치**: S1 32/32 · 25/25 · 고아 0 / S4 폴백 63 · 드리프트 **317** / S6 MISS 0 · slug 37 — **R2·R3과 동일**.

### R4-1. 접근성 검증 — 통과 (오탐 자체 차단 기록)

이번 라운드에서 접근성을 처음 실측했다. **처음에 "skip link 없음"으로 판정할 뻔했으나, 잘못된 파일(`Header.jsx`·`layout.js`)만 grep한 결과였다.** 전수 확인하니 정상 구현이다. R1의 F4 오탐과 같은 계열(측정 대상 착오)이라 발견으로 올리지 않고 통과로 기록한다.

| 항목 | 확인 결과 | 근거 |
|---|---|---|
| Skip link | **구현됨** — `SkipLink.jsx`(KR/EN 카피 분기) + `.skip-link:focus`로 포커스 시에만 노출 | `SkipLink.jsx:10-11`, `globals.css:4624-4625` |
| Skip 타깃 | **라우트 표면 전반 커버** — `id="main-content"` 17곳. 도구·SOP·랜딩은 디스패처 `app/[[...slug]]/PageClient.jsx`와 EN 짝이 보유, 블로그/용어/템플릿/진단/정책/계산기/주간은 각 레이아웃이 보유 | 전수 grep |
| async CSV live semantics | **구현됨** — `role="status" aria-live="polite" aria-atomic="true"` + 오류 `role="alert"`. ARCHITECTURE의 계약 주장이 실제와 일치 | `CsvUploader.jsx:602·657·709·790` |
| EN 문서 언어 | **정상** — 루트는 `lang="ko"`지만 `/en` 레이아웃이 인라인 스크립트 + `<div lang="en" data-locale-root="en">` 서브트리 + 소프트 내비 동기화로 처리. **JS 없이도 서버 HTML의 EN 서브트리 언어가 맞다** | `app/en/layout.js` |
| `:focus-visible` | globals.css에 29개 규칙 | S 측정 |

### R4-2. 신규 발견

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F19 | L2 | **S4 스윕의 사각지대 — `globals.css`가 측정 대상이 아니다.** S4는 `src/components`만 순회하는데, `globals.css` 규칙 본문에 토큰이 아닌 하드코딩 hex가 **228건** 있다(토큰 정의부 `:root`/`body.light-mode` 205건은 정상). F8(컴포넌트 317건)과 **별개 모수**이므로, 지금까지 보고된 드리프트 규모는 실제보다 작게 잡혀 있었다. 예: `.skip-link`의 `#11141b`·`#d8ff67` | 측정 | `globals.css` 전수 | P2 |

> 주의: 228건 전부가 결함은 아니다. `::selection`·그림자처럼 테마 무관 값이 섞여 있다. **"테마 전환 취약 후보"이지 확정 결함이 아니며**, 분류 없이 일괄 치환하지 않는다(R1의 F8 판단과 동일 기준).

### R4-3. 관찰 — 결함 아님

- **클라이언트 컴포넌트 비율**: 전체 151개 중 `"use client"` 94개. 다만 **app 라우트는 40개 중 4개만 client**로, 페이지는 서버 우선이고 클라이언트는 리프 UI에 몰려 있다. App Router 구조로는 정상이며 결함으로 올리지 않는다. 실제 초기 번들·LCP 판정은 빌드 산출물이 필요한데 이 라운드는 S7 미실행이라 **측정 불가**.

### R4-4. 프로세스 — 감사 대비 반영 지연 (R3에서 예고한 기록)

R2·R3·R4 **3라운드 연속으로 감사 브랜치에 코드 변경이 없다.** 현재 미해결 발견은 F8·F13·F14·F15·F16·F17·F18·F19이며, 이 중 **F13·F14·F17·F18은 난이도 S**로 분류돼 있다.

이는 감사 품질 문제가 아니라 **라운드 편성 문제**로 보인다. Codex 회차는 `:30`, 이 감사는 `:00` 기준이라 두 작업이 같은 라운드 구간을 공유하지 않는다. 다만 이 판단은 커밋 타임스탬프에 근거한 **추론**이며, 실제 원인은 확인하지 않았다.

### R4-5. 아이디에이션

> **[아이디에이션]** — 검증되지 않은 제안. 발견(F)과 섞어 읽지 말 것.

**첫인상**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I18 | **라이트 모드를 "보고용 기본"으로 격상** — 마케터는 결과를 캡처해 보고서·슬랙에 붙인다. 흰 배경이 실무 기본이라 **라이트 모드 완성도가 곧 공유 품질**이다. F19의 228건을 정리할 때 다크 우선이 아니라 "캡처했을 때 보고서에 붙일 만한가"를 기준으로 잡는다 | 결과 캡처는 다크가 아니라 라이트로 찍힌다 | 테마 토글 사용률(특히 다운로드·캡처 직전), 라이트 모드 체류 비중 |

**재사용**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I19 | **판단 요약만 공유하는 링크** — 마케터는 혼자 쓰지 않는다. 결과 화면의 **결론·근거·다음 행동 텍스트만**(원본 행·업로드 데이터 절대 제외) URL에 담아 팀에 공유하고, 받은 사람은 "같은 분석 내 데이터로 해보기"로 진입한다. 공유자는 재방문, 수신자는 신규 유입이 된다 | 팀 공유가 재방문과 신규 획득을 동시에 만든다 | 공유 링크 생성률, 링크 경유 신규 세션의 분석 실행률 |

**폐기**

| 아이디어 | 폐기 사유 |
|---|---|
| 공유 링크에 업로드한 행·지표 원본을 인코딩해 수신자가 같은 화면을 그대로 본다 | 서버 전송은 아니지만 **URL은 브라우저 히스토리·리퍼러·메신저 미리보기에 남는다.** 사내 민감 데이터를 그런 경로에 흘리는 건 §2.2의 취지(사용자 CSV는 브라우저 메모리에만) 위반이다. I19는 **모델이 만든 요약 텍스트만** 담는 조건에서만 성립한다 |

### R4-6. 다음 라운드로 넘기는 것

- F19는 F8과 **모수가 다른 별개 측정**이므로, 두 수치를 합산해 "드리프트 545건"처럼 말하지 않는다. 분류 후 표면 단위 처리가 전제다.
- S4에 `globals.css` 포함 여부는 스윕 개정 사항이다. 포함시키면 R1~R4의 317이라는 시계열이 끊기므로, **기존 S4는 유지하고 별도 S10으로 추가**하는 편이 비교 가능성을 지킨다.
- 4라운드 연속 코드 변경이 없으면 R5에서 라운드 편성 자체를 재검토 대상으로 올린다.

---

## Round 5 — Claude 감사 (2026-08-02 06:00 KST)

**동기화**: 감사 브랜치 `bba0ee7` = origin, `origin/main`과 차이 0.
**이 라운드 구간 코드 변경**: R4(`bba0ee7`) 이후 커밋 **0건**. **4라운드 연속**이다.
**신규 기능 딥다이브**: 대상 없음.
**스윕 수치**: S1 32/32 · 25/25 · 고아 0 / S4 폴백 63 · 드리프트 **317** / S6 MISS 0 · slug 37 — **R2~R4와 동일**.

### R5-1. L3 KR/EN 등가성 — 통과 (신규 실측, 강한 신호)

`CLAUDE.md §2.11`은 외부 노출 변경 시 KR/EN 동시 반영을 절대 원칙으로 둔다. 지금까지 라운드에서 판정 기준으로만 적고 재지 않았던 항목이다. `tx("KR","EN")`·`tr("KR","EN")` 2인자 문자열 리터럴을 전수 추출해 측정했다.

| 지표 | 값 | 해석 |
|---|---|---|
| tx/tr 2인자 리터럴 총계 | **2,414쌍** | 외부 노출 문자열이 이 규모로 이중화돼 있다 |
| **EN 인자에 한글 잔존** | **0건** | **영문 UI로 한글이 새는 지점이 없다** — §2.11이 실제로 지켜지고 있다 |
| EN 인자 비어 있음 | 9건 | 표본 확인 결과 **의도된 값**이다(한국어 단위 "명"·"개"가 영문에선 단위어 없음) |
| KR == EN | 15건 | 표본 확인 결과 **의도된 값**이다(`NEXT QUESTION`·`baseline` 등 한국어 UI에서도 영문 그대로 쓰는 표기) |

R2의 F16과 합치면 방향이 명확하다: **영문 쪽은 깨끗하고, 문제는 한국어 쪽에 영문 전문용어가 남아 있는 것**이다(adstock·carryover). 번역 누락이 아니라 **평이한 한국어 대체어 부재**가 과제다.

### R5-2. L4 정직 처리 — 측정 불가 판정 (수치 주장 안 함)

"추정 불가"류 정직 처리 커버리지를 재려 했으나 **단일 어휘 grep으로는 측정할 수 없다**는 결론이다.

1차 측정에서 `CampaignPvm`(5-21)·`MarketingEfficiency`(5-22)가 0건으로 나왔다. 그러나 다른 어휘로 확인하니 두 도구 모두 처리가 있다 — 5-21은 "최소 2"(`:259`)·"부족"(`:1508`), 5-22는 "신뢰도"·"한계"(`:417`) 등. **0건은 결함이 아니라 내 어휘 목록이 좁았던 것**이다. 매퍼(`AhaColumnMapper`·`MmmColumnMapper`)와 9~11줄 얇은 래퍼(`KillerContentFinder` 등)의 0건은 애초에 결과를 산출하지 않으므로 정상이다.

R1의 F4, R4의 skip link에 이어 **세 번째로 같은 계열 오탐을 발행 전에 차단**했다.

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F20 | L4 | **정직성 문구에 표준 어휘가 없다.** "추정 불가"·"판정 불가"·"부족"·"신뢰도"·"한계"·"최소 N" 등 도구마다 다른 표현을 쓴다. 감사 측에서 커버리지를 측정할 수 없다는 것보다 **사용자가 같은 상태(=결론 낼 근거가 없음)를 도구마다 다른 말로 만난다**는 게 실제 문제다. 제품의 생명선인 정직성이 일관된 신호로 인지되지 않는다 | 관찰 | 도구 전수 어휘 대조 | P2 |

### R5-3. 프로세스 — 라운드 편성 재검토 (R4에서 예고)

R2·R3·R4·R5 **4라운드 연속 코드 변경 0건**이다. 현재 미해결 발견은 F8·F13~F20 **9건**이며 이 중 **F13·F14·F17·F18은 난이도 S**다.

남은 라운드는 R6~R9 **4회**뿐이다. 지금 구조로는 "9라운드 감사 + 9라운드 반영"이라는 목표 중 반영 축이 R1 1회로 끝난다. 다만 이는 **커밋 타임스탬프에 근거한 추론**이며, 다른 작업 쪽의 실제 진행 상황은 이 세션에서 확인할 수 없다.

감사 측에서 할 수 있는 것은 **난이도 S 4건을 우선순위 최상단에 명시적으로 올려두는 것**뿐이다(§6 백로그와 별개로 여기 다시 적는다):

| 우선 | 항목 | 난이도 | 한 줄 요약 |
|---|---|---|---|
| 1 | **F13** | S | `ARCHITECTURE.md:49`·`CLAUDE.md:73`의 죽은 `5-6` 표기 제거 |
| 2 | **F14** | S | `CONNECTED_TOOLS`·`NEXT_TOOL_IDS`에 9-2·9-3·9-7 추가 |
| 3 | **F18** | S | `adjust-vs-appsflyer` 용어 연결 + `deep-link`·`ecpi` relatedPosts |
| 4 | **F17** | S | 5-18 결과 카드에 결정 기록 진입점 부여 |

### R5-4. 아이디에이션

> **[아이디에이션]** — 검증되지 않은 제안. 발견(F)과 섞어 읽지 말 것.

**시장 가치**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I20 | **이미 만들어둔 EN 자산을 실제로 쓴다** — EN 블로그 32편·용어 25개·UI 문자열 2,414쌍이 **한글 잔존 0건**으로 완비돼 있다(R5-1). 추가 제작비 없이 영문 검색 유입을 시험할 수 있는 상태인데, 지금은 KR 트래픽만 전제로 운영한다. 영문 고의도 질의(`marginal roas calculator` 등) 몇 개만 골라 진입 경로를 정비해보는 실험 | EN 자산의 한계비용이 0에 가까우므로 유입 실험의 기대값이 제작 대비 높다 | GSC 영문 쿼리 노출·CTR, `/en` 세션의 분석 실행률. 실행률이 KR과 크게 다르면 자산은 있어도 맥락이 안 맞는 것 |

**재사용**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I21 | **"결론 낼 수 없음"을 표준 배지로** — F20을 문구 통일로 끝내지 않고, 근거 부족 상태를 **모든 도구가 같은 모양의 한 배지**로 표시한다(예: `근거 부족 — 왜?`). 거짓 숫자를 안 만드는 제품이라는 인상이 재방문의 근거가 된다. 아무 결론도 못 준 화면이 오히려 신뢰를 남기는 구조 | 마케터는 틀린 확신보다 정직한 보류를 더 신뢰하고, 그 신뢰가 재방문을 만든다 | 배지 노출 세션의 재방문율 vs 결론이 나온 세션. 차이가 없으면 신뢰→재방문 가정이 약한 것 |
| I22 | **어휘 표준을 사용자 쪽으로 뒤집기** — F20의 표준화를 내부 규약이 아니라 **용어사전 25개와 연결**해 배지·경고 문구를 클릭하면 "왜 근거가 부족한지"가 용어 정의로 이어지게 한다(I12·I15와 같은 축) | 정직한 보류는 설명이 붙어야 신뢰가 되지, 그냥 비면 고장으로 읽힌다 | 배지 → 용어 이동률 |

**폐기**

| 아이디어 | 폐기 사유 |
|---|---|
| 근거가 부족할 때도 "참고용 추정치"를 흐린 글씨로라도 보여준다 | `CLAUDE.md §8`·claude-ux 정직성 위반. 흐린 글씨여도 화면에 뜬 숫자는 인용된다. 근거가 없으면 숫자를 만들지 않는 것이 이 제품의 차별점이고, 그걸 깨면 I21의 신뢰 자산도 같이 무너진다 |

### R5-5. 다음 라운드로 넘기는 것

- 미측정으로 남은 렌즈: 빈 상태·오류 복원(L1), OG 이미지 커버리지(L7-a), SOP 콘텐츠 품질(L3). R6~R8에서 하나씩 본다.
- R9는 종합 라운드이므로 신규 측정보다 9라운드 대조에 지면을 쓴다.

---

## Round 6 — Claude 감사 (2026-08-02 07:00 KST)

**동기화**: 감사 브랜치 `2b35fd6` = origin, `origin/main`과 차이 0.
**이 라운드 구간 코드 변경**: R5(`2b35fd6`) 이후 커밋 **0건**. **5라운드 연속**이다.
**신규 기능 딥다이브**: 대상 없음.
**스윕 수치**: S1 32/32 · 25/25 · 고아 0 / S4 폴백 63 · 드리프트 **317** / S6 MISS 0 · slug 37 — **R2~R5와 동일**.

R5-5에서 배치한 대로 이번 라운드는 **오류 복원(L1)**과 **OG 커버리지(L7-a)**를 실측했다.

### R6-1. 신규 발견 — 오류 경계가 도구 라우트에만 있다

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F21 | L1 | **`error.js` 경계가 디스패처 라우트 2개에만 있다.** `app/[[...slug]]/error.js`·`app/en/[...slug]/error.js`가 `RouteErrorRecovery`로 브랜드 복구 UI를 제공하지만, **루트 `error.js`·`global-error.js`가 없고** `/blog` `/glossary` `/calculator` `/templates` `/diagnose` `/contact` `/privacy` `/terms` `/weekly-review` `/weekly-report` `/og` + EN 짝 **총 22개 세그먼트에 경계가 없다**. 이 라우트에서 렌더 오류가 나면 Next 기본 오류 화면으로 떨어져 복구 동선이 사라진다 | 측정 | 라우트 세그먼트 전수 | P2 |

**심각도를 P1으로 올리지 않은 이유**: 커버되지 않은 라우트 다수가 정적 콘텐츠(`/blog`·`/glossary`·`/privacy`·`/terms`)여서 런타임 렌더 오류 확률이 낮고, 오류 위험이 가장 큰 인터랙티브 도구·대시보드는 이미 경계 안에 있다. 다만 `/calculator`·`/diagnose`·`/weekly-review`·`/weekly-report`는 인터랙티브 표면인데 경계 밖이다.

**난이도 S**: 루트 `src/app/error.js` 하나를 추가하면 하위 전 세그먼트가 덮인다(이미 있는 `RouteErrorRecovery` 재사용).

### R6-2. L7-a OG 커버리지 — 통과

| 항목 | 확인 결과 | 근거 |
|---|---|---|
| 글별 OG 카드 | **32편 전부 커버** — `app/blog/[slug]/opengraph-image.js`와 EN 짝이 파일 컨벤션으로 자동 주입 | 라우트 파일 존재 |
| `ogImage` frontmatter | 4/32 — 나머지는 동적 카드로 폴백되므로 **누락이 아니라 override 4건** | `content/blog/*` |
| 전역 OG | `app/og-card.png/route.js` | ARCHITECTURE §1 |

`ogImage` 4/32를 "커버리지 12.5%"로 읽으면 오탐이다. 동적 라우트가 기본값을 주고 frontmatter는 개별 덮어쓰기이므로 **실질 커버리지는 100%**다. (R1 F4·R4 skip link·R5 L4에 이어 같은 계열 오독을 네 번째로 사전 차단.)

### R6-3. 아이디에이션

> **[아이디에이션]** — 검증되지 않은 제안. 발견(F)과 섞어 읽지 말 것.

**재사용**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I23 | **오류 화면을 이탈 지점이 아니라 탈출구로** — F21을 루트 `error.js` 추가로 끝내지 않는다. `RouteErrorRecovery`를 전 라우트에 확대할 때, 오류 화면에 **"방금 하려던 것"에 가까운 대안**을 제시한다(도구에서 났으면 같은 데이터로 가능한 다른 분석, 콘텐츠에서 났으면 관련 용어·글). 오류는 재방문을 가장 확실하게 깎는 경험이라 여기서의 회수가 크다 | 오류를 만난 사용자의 이탈은 오류 자체보다 "다음에 뭘 할지 없음"에서 온다 | 오류 화면 노출 세션의 후속 페이지뷰·재방문율. 대안 클릭률 |
| I24 | **주간 검토 라우트를 경계 안으로 우선 이동** — 경계 없는 22개 중 `/weekly-review`·`/weekly-report`는 **리텐션 루프의 종착지**다(R3에서 확인한 결정 인박스 동선). 하필 재방문자가 도달하는 화면이 오류 시 무방비다. F21을 처리할 때 이 두 라우트를 먼저 본다 | 재방문자가 만나는 오류는 신규 방문자의 오류보다 이탈 비용이 크다 | 해당 라우트 오류율·이탈률 |

**첫인상**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I25 | **소셜 OG를 "실제 결과 화면"으로** — 전역 OG(`og-card.png`)는 실제 제품 UI를 쓰는데, 블로그 글별 카드는 텍스트 기반이다. 글 주제에 맞는 **실제 분석 결과 미니어처**를 얹으면 "이 사이트에서 저런 화면이 나온다"가 공유 시점에 전달된다. 랜딩의 `ToolCardMock`·`ProductPreview`가 이미 결정론적 목업을 만들고 있어 재활용 여지가 있다 | 소셜 타임라인에서 도구 화면 이미지가 텍스트 카드보다 클릭을 더 만든다 | 글별 OG 교체 전후 소셜 유입 CTR(GSC·GA4 referrer) |

**폐기**

| 아이디어 | 폐기 사유 |
|---|---|
| 오류 화면에 "문제가 자동 보고되었습니다" 문구를 넣어 신뢰를 준다 | 현재 오류 리포팅 파이프라인이 없다. 동작하지 않는 기능을 카피로 주장하는 것은 `CLAUDE.md §8` 위반이며, 이 제품이 지켜온 "동작 안 하는 카피 금지" 원칙과 정면으로 어긋난다. 리포팅을 실제로 붙인 뒤에만 쓸 수 있는 문구다 |

### R6-4. 다음 라운드로 넘기는 것

- 미측정 잔여: SOP 콘텐츠 품질(L3), 도구 빈 상태 카피 일관성(L1·L3). R7~R8에 배치한다.
- 5라운드 연속 반영 없음. 미해결 발견은 F8·F13~F21 **10건**이고 난이도 S는 **F13·F14·F17·F18·F21 5건**으로 늘었다.

---

## Round 7 — Claude 감사 (2026-08-02 08:00 KST)

**동기화**: 감사 브랜치 `8362cc0` = origin, `origin/main`과 차이 0.
**이 라운드 구간 코드 변경**: R6(`8362cc0`) 이후 커밋 **0건**. **6라운드 연속**이다.
**신규 기능 딥다이브**: 대상 없음.
**스윕 수치**: S1 32/32 · 25/25 · 고아 0 / S4 폴백 63 · 드리프트 **317** / S6 MISS 0 · slug 37 — **R2~R6과 동일**.

R6-4에서 배치한 대로 **SOP 콘텐츠(L3)**를 실측했다.

### R7-1. 오탐 사전 차단 — 이번 감사에서 가장 컸을 오보

측정 중간에 다음 사실이 나왔다.

- `public/content/pages/`의 한글 JSON은 **`1-1.json`·`8-1.json` 단 2개**, 나머지 15개는 전부 `*.en.json`
- 클라이언트 로더는 **EN만 폴백을 가진다** — `en`은 `.en.json` 실패 시 `.json`으로 폴백하지만, `ko`는 `.json` 단독 시도 후 실패하면 `null`(`SopContent.jsx:808-833`)
- `renderPageData`는 `data`가 없으면 `null` 반환(`:924-925`)

여기까지만 보면 **"KR SOP 12개 라우트가 빈 화면"이라는 P0**로 직행하는 그림이다. 실제로 그렇게 기록할 뻔했다.

**틀렸다.** 확인해보니 **한국어 SOP는 JSON이 아니라 하드코딩된 `page_N_N()` 함수로 렌더된다.** `SopContent.jsx`에 `page_1_1`~`page_8_1` **16개 함수가 모두 존재**하며, JSON 경로는 EN 전용이다(`:3075-3077` 주석이 "locale=ko 동기 경로는 이 분기 밖"이라고 명시). KR 화면은 정상이다.

R1 F4(측정 레이어) → R4 skip link(잘못된 파일) → R5 L4(좁은 어휘) → R6 OG(폴백 구조 미인지) → **R7 SOP(렌더 경로 미인지)**. 다섯 번째이자 **가장 피해가 컸을 오보**를 발행 전에 차단했다. 공통 원인은 하나다 — **파일·문자열만 보고 실제 렌더 경로를 확인하지 않는 것**.

### R7-2. 신규 발견

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F22 | L3·횡단 | **SOP의 KR/EN 렌더 경로가 이원화돼 있다.** KR은 `SopContent.jsx`(3,183줄) 안의 하드코딩 `page_N_N()` 함수 16개, EN은 `public/content/pages/*.en.json` 15개다. 같은 문서의 두 로케일이 **서로 다른 저장소·다른 편집 방식**을 갖는다. `CLAUDE.md §2.11`(외부 노출 KR/EN 동시 반영)이 구조적으로 어렵다 — KR 수정은 코드 변경, EN 수정은 데이터 변경이라 한쪽만 바뀌어도 테스트가 못 잡는다. 실제로 KR `.json`은 `1-1`·`8-1` 2개만 남아 EN 폴백용 잔재로 보인다 | 측정 | `SopContent.jsx` 함수 전수, `public/content/pages/` 파일 전수 | P2 |
| F23 | 횡단 | **죽은 렌더러 `page_4_4()`.** 함수는 존재하지만 `routeMap`에도 `IA`에도 `4-4`가 없어 도달 불가다. `CLAUDE.md §7`은 "페이지 제거 = renderer 함수 + 등록 통째 삭제"를 명시하며, 죽은 렌더러 안의 중복 id·공유 클래스가 cross-page 버그의 불씨가 된다고 기록한다 | 측정 | `SopContent.jsx` 함수 목록 vs `routeMap.js`·`useDataStore.js` | P3 |

`8-1`은 `routeMap.js:54`·IA 모두에 있어 정상이다(4-4와 달리 죽은 렌더러가 아니다).

### R7-3. 아이디에이션

> **[아이디에이션]** — 검증되지 않은 제안. 발견(F)과 섞어 읽지 말 것.

**첫인상**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I26 | **SOP를 "읽을거리"가 아니라 도구의 사전 단계로** — 지금 SOP(가이드 14편)와 분석 도구는 별개 표면이다. 각 SOP 말미에 "이 문서대로 준비한 데이터로 지금 분석하기"를 넣어 **문서 → 도구 진입**을 만든다. F14(콘텐츠 도구 막다른 길)·I15(용어→도구)와 같은 축이며, SOP는 이미 CSV 준비 방법을 설명하고 있어(`8-1` CSV 데이터 준비 가이드) 연결 논리가 자연스럽다 | SOP를 읽는 사람은 실행 직전 단계에 있다 | SOP → 도구 진입률, 진입 후 업로드 완료율 |

**재사용**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I27 | **F22를 기회로 — SOP를 단일 소스로 통합하면 EN 시장이 열린다** — 지금은 KR 코드/EN 데이터라 SOP 한 편 고치는 비용이 두 배다. 한쪽(데이터)으로 통합하면 유지비가 절반이 되고, R5의 I20(이미 완비된 EN 자산 활용)과 합쳐 **영문 SOP를 실제 유입 자산으로 쓸 수 있다.** 통합 자체는 리팩터링이지만 목적은 시장 확장이다 | SOP 유지비가 두 배인 것이 EN 콘텐츠 갱신을 막고 있다 | 통합 전후 SOP 갱신 빈도, `/en/guide` 유입·체류 |

**폐기**

| 아이디어 | 폐기 사유 |
|---|---|
| KR SOP도 JSON으로 옮기는 김에 문서를 AI로 일괄 재작성해 분량을 늘린다 | SOP는 이 제품의 실무 신뢰 자산이고 현재 EN JSON도 최소 4.5KB로 얇지 않다. 검증되지 않은 자동 생성 문장을 실무 SOP에 섞는 것은 `CLAUDE.md §8`(거짓 정보 금지)과 AAO/GEO 운영 기준(실제 근거만 인용)에 어긋난다. 통합은 **형식 이관**이지 내용 생성이 아니다 |

### R7-4. 다음 라운드로 넘기는 것

- 미측정 잔여: 도구 빈 상태 카피 일관성(L1·L3). R8에서 본다.
- 6라운드 연속 반영 없음. 미해결 발견 **12건**(F8·F13~F23), 난이도 S는 F13·F14·F17·F18·F21 **5건** 유지 + F23(죽은 렌더러 삭제)도 S급이다.
- R9는 종합 라운드이므로 신규 측정을 최소화하고 9라운드 대조·아이디에이션 정리에 지면을 쓴다.

---

## Round 8 — Claude 감사 (2026-08-02 09:00 KST)

**동기화**: 감사 브랜치 `d8af742` = origin, `origin/main`과 차이 0.
**이 라운드 구간 코드 변경**: R7(`d8af742`) 이후 커밋 **0건**. **7라운드 연속**이다.
**신규 기능 딥다이브**: 대상 없음.
**스윕 수치**: S1 32/32 · 25/25 · 고아 0 / S4 폴백 63 · 드리프트 **317** / S6 MISS 0 · slug 37 — **R2~R7과 동일**.

R7-4에서 배치한 마지막 미측정 렌즈 **도구 빈 상태 진입 안내(L1·L3)**를 실측했다.

### R8-1. 진입 안내 커버리지 — 통과 (여섯 번째 오탐 차단)

1차 측정은 이렇게 나왔다.

- `TOOL_GUIDE` 키 13개 vs IA 도구 13개인데 **집합이 다르다** — 가이드에는 죽은 `5-6`이 있고 실제 도구 `5-23`(증분 분석)이 없다
- `getToolGuide`는 미등록 키에 `null`을 반환하고(`toolGuide.js:525-528`), `CsvGuide`는 `guide`가 없으면 **`null`을 반환해 아예 렌더되지 않는다**(`CsvGuide.jsx:58-59`)

즉 "**증분 분석 도구는 업로드 안내가 통째로 안 뜬다 (P1)**"로 직행하는 그림이다.

**틀렸다.** `Incrementality.jsx:231`은 `toolId={`5-23:${method}`}` 형태의 **복합 키**를 넘긴다. `TOOL_GUIDE`에는 `5-23:suppression`·`5-23:on`·`5-23:off`가 KR/EN 양쪽에 모두 있고(`:137·155·169`, EN `:362·380·394`), `method` 상태값도 정확히 `suppression|on|off`다(`:88·182-184`). **증분 분석은 방법별로 오히려 더 세밀한 안내를 갖는다.** 내 정규식이 `"5-23"` 형태의 단순 키만 찾아 복합 키를 놓쳤다.

| 항목 | 결과 |
|---|---|
| 진입 안내 커버리지 | **13/13 도구 전부** (5-23은 방법별 3개로 세분) |
| KR/EN | `TOOL_GUIDE`·`TOOL_GUIDE_EN` 양쪽 존재 |

### R8-2. 신규 발견

| ID | 렌즈 | 증상 | 등급 | 근거 | 심각도 |
|---|---|---|---|---|---|
| F24 | 횡단 | **죽은 `5-6` 키가 `toolGuide.js`에도 남아 있다** — KR `:75`, EN `:317`. F13(ARCHITECTURE.md:49)·F13(CLAUDE.md:73)에 이은 **세 번째 위치**다. 5-6은 9-6으로 통합돼 `routeMap`에 없으므로 이 가이드는 도달 불가 데이터다 | 측정 | `toolGuide.js:75·317` vs `routeMap.js` | P3 |

F13을 처리할 때 문서 2곳만 고치면 이 항목이 남는다. **죽은 id는 문서·데이터·코드 세 곳에 흩어져 있다.**

### R8-3. 메타 — 스윕 기반 감사의 체계적 오탐률

R1부터 R8까지 **여섯 번 연속으로, 스윕이 만든 1차 결론이 실제 코드 확인에서 뒤집혔다.**

| 라운드 | 1차 결론(오탐) | 실제 | 놓친 것 |
|---|---|---|---|
| R1 | EN 설명 15건 잘림 | 0건 | 측정 대상이 원고 frontmatter, 실제는 `blogSeo.js` |
| R4 | skip link 없음 | 구현됨 | 잘못된 파일만 grep |
| R5 | 5-21·5-22 정직 처리 0건 | 있음 | 어휘 목록이 좁음 |
| R6 | OG 커버리지 12.5% | 100% | 동적 라우트 폴백 미인지 |
| R7 | KR SOP 12개 빈 화면 | 정상 | 렌더 경로가 JSON이 아니라 하드코딩 함수 |
| R8 | 5-23 안내 없음 | 방법별 3개 | 복합 키를 단순 키 정규식으로 검색 |

**여섯 건 모두 같은 실패 양식**이다 — *문자열·파일 이름만 보고 실제 소비 경로를 확인하지 않음*. R1의 F4만 발행됐고(Codex가 교정) 나머지 다섯은 발행 전에 차단됐다.

이는 **§3 스윕의 구조적 한계**다. 스윕은 후보를 만들 뿐 판정하지 못한다. §4 증거등급의 `측정`은 "스윕이 숫자를 냈다"가 아니라 **"소비 경로까지 확인했다"**를 뜻해야 한다. R9 종합에서 이 기준을 명시적으로 남긴다.

### R8-4. 아이디에이션

> **[아이디에이션]** — 검증되지 않은 제안. 발견(F)과 섞어 읽지 말 것.

**첫인상**

| ID | 아이디어 | 가정 | 검증 방법 |
|---|---|---|---|
| I28 | **5-23의 "방법 먼저, 안내는 그다음" 패턴을 다른 도구로** — 증분 분석은 방법(통제군·신규켜기·종료)을 첫 행동으로 고르게 하고 그 선택에 맞는 안내만 보여준다(`Incrementality.jsx:141`). 도구 하나에 여러 용법이 있는 5-18(진단·기여·예측)·5-2(9탭)에도 같은 형태를 적용하면, 첫 화면의 선택지가 "탭 목록"이 아니라 "지금 뭘 하려는가"가 된다 | 사용자는 기능 이름보다 의도로 진입한다. 랜딩이 이미 질문 3개로 진입시키는 것과 같은 논리 | 진입 후 첫 클릭까지 시간, 탭 이탈률 |
| I29 | **템플릿을 "받는 것"이 아니라 "바로 돌려보는 것"으로** — 각 도구에 템플릿 CSV 다운로드가 이미 있다(`onDownloadTemplate`). 다운로드는 이탈 지점(엑셀로 나감)이므로, 같은 자리에 **"템플릿 그대로 분석해보기"** 원클릭을 둬 결과 화면을 먼저 보여준다. R2의 I1·I3과 같은 축(업로드 마찰을 뒤로 미루기) | 다운로드받은 사람 상당수는 돌아오지 않는다 | 템플릿 다운로드 대비 즉시분석 클릭 비율, 이후 실데이터 업로드 전환율 |

**폐기**

| 아이디어 | 폐기 사유 |
|---|---|
| 안내가 없는 도구를 자동 탐지해 가이드 문구를 생성해 채운다 | R8-1이 보여주듯 "안내 없음" 판정 자체가 오탐이었다. **틀린 탐지 위에 자동 생성을 얹으면 잘못된 안내가 실제 화면에 나간다.** 가이드는 도구의 실제 컬럼 계약을 아는 사람이 쓰는 것이고, `TOOL_GUIDE`는 §12.21이 정한 수기 SSOT다 |

### R8-5. 다음 라운드(R9 종합)로 넘기는 것

- 미측정 렌즈는 계획한 범위에서 모두 소진했다. R9는 **신규 측정 최소화**, 9라운드 대조·아이디에이션 정리에 지면을 쓴다.
- 7라운드 연속 반영 없음. 미해결 발견 **13건**(F8·F13~F24), 난이도 S는 F13·F14·F17·F18·F21·F23·F24 **7건**.
- R9에서 남길 것: ① 라운드별 감사↔코딩 대조표 ② 스윕 수치 추이 ③ 끝내 남은 항목과 이유 ④ 아이디에이션 채택·폐기 현황 ⑤ R8-3의 증거등급 기준 개정 제안.

---

## Round 2–8 — Codex 실행 응답 (2026-08-02 09:45 KST)

> 기존 감사 내용은 수정·삭제하지 않았다. R2~R8 후보를 실제 라우트·소비 컴포넌트·렌더 조건까지 다시 확인한 뒤, 사용자 승인 범위만 구현했다.

### 1. 즉시 처리 완료

| 항목 | 반영 | 확인 |
|---|---|---|
| F13·F24 | 현재 도구 표기의 `5-6`을 통합된 `9-6`으로 바로잡고, 도달 불가능한 KR/EN `TOOL_GUIDE["5-6"]` 제거. 과거 장애 기록의 5-6 표기는 역사적 근거라 보존 | `routeMap`의 실제 `/content/freshness`와 문서·가이드 정합 |
| F23 | 라우트·IA에서 도달할 수 없는 `page_4_4()` 본문과 렌더러 등록 제거 | `SopContent.jsx`에서 `page_4_4`·`4-4` 참조 0건 |
| F21 | 일반 KR/EN 페이지용 `app/error.js`·`app/en/error.js`, root layout용 `global-error.js` 추가. 기존 분석 오류 복구 카피와 일반 페이지 복구 카피를 분리 | 일반 페이지 오류에서 잘못된 “분석 허브” 문구가 나오지 않음 |

### 2. 설계 후 처리 완료

| 항목 | 반영 | 가드레일 |
|---|---|---|
| F17 | 5-18의 추세·잠식·기여·예측 요약마다 결론·행동·가설·기준값·검토 질문을 안전하게 prefill하고 `DecisionReview` 활성화 | 데모에서는 저장 UI 미노출, 원본 행·파일명·차트 데이터는 기록 스키마 밖, 예측 미인증이면 예산 변경 “보류”만 제안 |
| F20 | `EvidenceStatusBadge`를 공통 컴포넌트로 추가해 `사용 가능 / 주의해서 해석 / 근거 부족 / 식별 불가 / 판정 보류 / 계산 오류`를 같은 모양·설명으로 표시. 공통 데이터 근거 바와 5-18 결과 카드에 연결 | 근거 부족 시 참고 숫자를 새로 만들지 않으며, 포커스 가능한 설명으로 “왜 보류인지” 제공 |

### 3. 승인된 아이디에이션 6개 반영

1. **가능한 분석 먼저**: `/start`에서 업로드 직후 자동 매핑 기준 추천을 매핑 표 위에 표시한다. 추천 도구 선택 시 대상 도구 계약으로 다시 매핑한다.
2. **5-18 예측·기여 검토 루프**: F17과 함께 기준값 → 다음 실제값 → 배운 점으로 이어지게 했다.
3. **예시 즉시 실행**: 도구 가이드에 `예시 데이터로 결과 바로 보기`를 배치하고, 별도 다운로드·재업로드 없이 기존 결정론적 demo loader를 실행한다.
4. **주간 변화 압축**: 저장된 결정 요약의 baseline·actual 첫 숫자가 같은 단위일 때만 중립적인 델타와 변화율을 표시한다. 지표 방향의 좋음/나쁨은 임의 판정하지 않는다.
5. **첫 시작 비용 표시**: 도구 가이드와 데이터 라우터에 필수 항목 수와 `CSV 준비 제외·화면 작업 예상 범위`를 KR/EN으로 표시한다.
6. **근거 상태 표준화**: F20 공통 배지와 설명을 구현했다.

### 4. 이번에 진행하지 않은 항목 — 문제와 좋아지는 점

| 항목 | 현재 문제 | 진행하면 좋아지는 점 | 이번 보류 이유 |
|---|---|---|---|
| F22 SOP KR/EN 이원화 | KR은 3천 줄 JSX 함수, EN은 JSON이라 같은 문서를 두 편집 경로에서 관리한다. 한쪽만 수정돼도 현재 테스트가 의미 등가성을 보장하지 못한다 | 단일 콘텐츠 SSOT로 옮기면 수정 비용·번역 누락 위험이 줄고 KR/EN 회귀 테스트를 만들 수 있다 | 콘텐츠 byte 보존과 16개 라우트 대조가 필요한 별도 마이그레이션. 현재 UX PR에 섞으면 회귀 범위가 지나치게 커짐 |
| F15 DataTable 저채택 | 직접 `<table>`을 쓰는 도구가 많아 기본 접근성·모바일·빈 상태 패턴이 표마다 달라질 수 있다 | 단순 표부터 공통화하면 정렬·캡션·모바일·테마 품질을 한곳에서 개선 가능 | 드릴다운·셀 편집·복합 헤더 표까지 일괄 교체하면 기능 손실 가능. 표 유형 분류가 먼저 |
| F19 CSS 하드코딩 색상 후보 | `globals.css`의 일부 직접 색상이 테마 전환에서 대비가 흔들릴 가능성이 있다 | 실제 깨지는 규칙을 토큰화하면 다크/라이트 일관성과 유지보수가 좋아짐 | 집계된 228건은 결함 수가 아니라 후보 수. 재현 없이 일괄 치환하면 차트·상태색 의미를 훼손할 수 있음 |

F14(콘텐츠 preview 도구의 메인 여정 편입), F16(전문용어 노출 정책), F18(부분 오탐인 상호링크)은 이번 승인 범위 밖이라 변경하지 않았다.

### 5. 검증

- `npm run lint` — 통과
- `npm run test:all` — **167파일, 1,132개 통과, 1개 skip**
- `npm run build` — **220개 정적 페이지 생성 완료**
- KR/EN 카피·오류 경계·예시 실행·즉시 추천·검토 약속·기준 대비 변화 스모크 추가
